# 当前未关闭风险

最后更新：2026-06-17

## R1 运维入口仍需生产后台和告警通知闭环

- 状态：未关闭
- 说明：`docs/runbooks` 已建立，`operations-blueprint.md` 也已把整体 App / 后端 / 管理后台的 Codex 协助运维范围固定下来；ECS / RDS / Redis / OSS / DNS / HTTPS / 部署脚本、Go 请求级日志、ECS 日志查询脚本、农技千查专用 SLS Project / Logstore 已经有一批真实入口。第一版网页后台代码已落地为 Vite `admin` 前端 + `server-go` `/admin-api/v1/*`，并新增后台账号、session、CSRF、角色校验和审计；生产入口已部署到 `https://admin.nongjiqiancha.cn/`，Nginx 静态托管并同域反代 `/admin-api/`，后台域名 HTTPS 证书已签发，owner 账号已通过一次性 bootstrap 初始化，bootstrap 环境变量已从 ECS 清理。
- 风险：生产后台已上线并能登录，SLS 也已接入 5 条最小 AlertHub 告警，并可用 `scripts/check-sls-alert-readiness.ps1` 只读巡检规则启用、查询语句、触发条件、重复提醒、行动策略和仪表盘绑定状态，但还不能把“长期运营后台完全闭环”写成完成态；2026-06-13 云上巡检确认 5 条规则均启用且进入 AlertHub，SLS 应用日志行动策略 `nongji-prod-email` 和仪表盘 `nongji-prod-ops` 绑定已为 `5/5`，严格模式当前应通过；首封告警邮件仍需在真实或测试触发时确认送达。阿里云云监控已另建邮件联系人组 `NongjiQianchaOps` 和 9 条 ECS / RDS / Redis 资源水位告警，资源不足会先走邮件提醒；2026-06-14 已按官方推荐给 ECS 补装 CloudMonitor C++ 插件，ECS 上 `cloudmonitor.service` 已 running，本轮严格资源巡检显示 ECS 内存告警已从 `INSUFFICIENT_DATA` 回到 `OK`，后续脚本会继续把无数据状态暴露为 warning / attention，避免资源告警假绿；数据库只读脚本、后台账号受控重置 / 禁用 / 角色管理、客服正式坐席分配 / 标签、礼品卡批量发放 / 发放对象管理、检查更新 APK 上传 / 完整回滚入口等仍需继续补。
- 补充：2026-06-13 严格资源巡检已把 SLS 查询语句、严重级别、触发条件、重复提醒、行动策略 ID、仪表盘 ID 和 runbook 注解 warning 漂移纳入失败条件；这能减少“告警还在但规则被改偏”的假绿。首封 SLS 告警邮件仍需真实或测试触发确认送达。
- 补充：2026-06-17 内部 debug 测试包下载已切到 `download.nongjiqiancha.cn + OSS private object + signed URL` 低成本主链；阿里云 OSS 默认公网 endpoint 仍不能作为 APK 分发地址，也不能把 `*.oss-cn-beijing.aliyuncs.com` 签名 URL 发给测试用户。发布脚本裸跑默认使用 OSS 签名下载并让 OSS `test-apks/debug/` 只保留最新测试包，走 OSS 签名下载时也清掉 ECS 旧 `/test-apks/` 调试包镜像；`https://nongjiqiancha.cn/test-apks/...` 仅保留为显式 `-UseEcsDownloadFallback` 的临时回退路径，不是官网正式下载入口，也不是 App 内检查更新物料。后续每次发测试包仍必须让发布脚本校验签名 HEAD、文件大小和 SHA-256。早期证书同步脚本曾把下载域名私钥带入 Cloud Assistant 输出，已按泄露处理重新签发 `download.nongjiqiancha.cn` 免费证书并同步到 OSS；旧 Cloud Assistant 执行输出按可能留存处理，后续不再信任旧证书。现 `scripts/sync-oss-download-certificate.ps1` 改为临时公钥加密回传证书载荷，不再把明文私钥写入 Cloud Assistant 输出。`download.nongjiqiancha.cn` 的 ECS Let’s Encrypt 免费证书会自动续期，但 OSS CNAME 证书绑定仍需要脚本同步和到期提醒；当前已创建本机每周续费 / 证书巡检自动化提醒。正式 release 包仍要走 `app-update.md` 的 release 物料校验、后台配置、旧包覆盖安装和回滚流程，且不能使用 72 小时测试短签名链接。
- 补充：2026-06-13 继续巡检监控面板和服务健康页时，已把后台可见 SLS 文案统一到当前状态：5 条 AlertHub 最小告警已绑定邮件行动策略和最小仪表盘，ECS / RDS / Redis 资源水位另走云监控邮件；旧的“外部通知、资源水位和仪表盘仍需继续补”口径已从页面和监控 notes 中移除，并新增单测防回潮。同日新增 `scripts/check-public-blackbox.ps1`，从公网直接检查 API、官网、www、后台首页、后台未登录鉴权和 HTTP->HTTPS 跳转，首次运行 `status=ready`。剩余仍是把黑盒探测接成自动定时通知、首封 SLS 告警邮件送达确认、更细趋势和完整 Nginx access 聚合。
- 补充：管理后台总方案见 [management-backend.md](D:/wuhao/docs/runbooks/management-backend.md)，详细页面设计见 [admin-dashboard-design.md](D:/wuhao/docs/runbooks/admin-dashboard-design.md)，架构决策见 [ADR-0004-admin-backend-architecture.md](D:/wuhao/docs/adr/ADR-0004-admin-backend-architecture.md)。后台当前覆盖登录、账号安全、总览、监控面板、产品洞察、用户、会员额度、订单、礼品卡、帮助反馈、App 日志、今日农情、检查更新、审计和服务健康；账号安全已支持登录后自助改密和 `must_change_password` 强制改密硬兜底，后台登录也新增“用户名 hash + IP hash”专用限流保护口令入口；监控面板已聚合真实业务表、App 自动日志、审计、健康状态和地区分布，并补了“当前结论 / 就绪-需处理-阻塞 / 上线人工确认项 / 登录与账号ID / 礼品卡与权益 / 客服反馈 / App质量”决策卡，以及登录排障、登录阶段漏斗、检查更新排障卡和上线前真机回归清单；产品洞察首版已补脱敏聚合趋势、反馈主题命中、App 事件分类、Top App 事件和礼品卡失败原因，不返回聊天全文、反馈正文、图片 URL、手机号、token、模型 Key 或礼品卡完整码；礼品卡后台已补汇总、完整卡码加密保存后页面查看复制、尾号 / 批次 / 账号ID追溯和失败原因聚合，owner / finance_ops 可看完整卡码，owner / support / finance_ops 可看完整手机号；帮助反馈已补 open / replied / closed 队列、状态筛选、搜索、关闭和重开，客服图片展示收紧到同源 support 图片，状态备注会拒绝敏感全文；今日农情补跑、检查更新发布 / 停更、检查更新发布历史、礼品卡作废、反馈状态更新等写操作已有二次确认或按钮忙碌态和失败弹窗，长字段会换行，降低管理层试用时的误操作和表格撑破风险。新增 `scripts/check-admin-surface.mjs` 只读巡检前端路由、按钮动作、后台 API 调用和服务端授权矩阵是否漂移；新增 `scripts/check-admin-authenticated-smoke.ps1`，可用 owner 凭据做登录后只读核心 API smoke，但本机当前没有后台密码，尚未真实跑通这项。真机回归清单只把登录、聊天、图片、礼品卡、今日农情、检查更新和反馈入口串起来辅助测试，不代表这些 Android 真机链路已经全部验收完成。SLS 已有 `nongji-server-5xx`、`nongji-server-slow`、`nongji-nginx-upstream`、`nongji-daily-agri-failed`、`nongji-model-auth-config` 5 条 AlertHub 最小告警，已绑定邮件行动策略和最小仪表盘，其中正常 200 的 `/api/chat/stream` 长连接已从慢请求事件分流为 `http_sse_stream`，避免污染慢请求告警；但第一封 SLS 告警邮件送达确认、登录后 smoke 实跑、更细按版本 / 设备 / 网络环境的登录转化趋势、检查更新 APK 上传 / 完整回滚入口、产品洞察日报 / 人工标签 / 处理状态仍未接入；内部共享密钥接口仍保留给脚本兼容，但浏览器后台不应持有内部 secret。
- 补充：2026-06-15 继续收口后台误操作风险：帮助反馈状态文案从“已回复”改为“已处理/无需回复”，避免把未发送用户消息的状态变更误读成已经回复用户；前端要求处理备注，后端在最新用户消息尚无后台回复时拒绝无备注标记。礼品卡生成已增加真实权益提醒，确认字段已收紧为“张数 + 档位 + 天数”，例如 `3 Pro 30`；检查更新启用确认会展示 SHA-256、文件大小并提醒先跑 release-match 校验，停更空配置也不再显示为非法配置。`check-admin-surface.mjs` 已把这些确认项纳入静态巡检。剩余风险仍是完整客服坐席 / 标签 / 通知、检查更新 APK 上传和完整回滚入口、礼品卡发放对象管理尚未完成。
- 补充：2026-06-14 巡检发现用户早上真机测试包的 `auth.app_crash / app.crash` 已能补报到后台，但旧包摘要里异常类名 / 栈顶类名不完整，只剩方法名和行号时定位效率不足。Android 和后端已补安全崩溃诊断字段保留，只允许异常类名、栈顶类 / 方法 / 行号等形态通过，URL、手机号、token、正文和图片信息仍丢弃；这能改善后续排障质量，但不能补全已经上报的旧日志，仍需用户安装新包后再次真机验证登录协议页、主聊天发送和额度耗尽重试链。
- 补充：2026-06-15 已新增只读闪退摘要脚本 `scripts/query-ecs-app-crashes.ps1`，用于通过 Cloud Assistant 在 ECS 内部查询生产 `client_app_logs` 的 `app.crash / auth.app_crash`，只输出崩溃签名、包类型、版本号、系统版本、设备型号、脱敏用户类型和安全栈顶线索，不输出完整账号ID、手机号、token、图片 URL、正文、完整 attrs 或模型 Key；`docs/runbooks/app-client-logs.md` 已同步使用方式。最近 72 小时实测最新闪退补报停在 2026-06-14 17:47 的旧 debug 包阶段，用户 2026-06-14 21:31 安装新包后当前未见更晚闪退补报。监控页顶部也新增“处理顺序”区并把若干工程标题改成业务负责人更容易理解的表达；`/admin-api/v1/monitoring` 现会在 `auth_logs.latest_crash_at` 返回最近 24 小时闪退补报的最新时间，页面直接显示“最近闪退：时间 / 24h 无新闪退”，帮助区分旧包噪声和新包当前状态。但这仍不等于真机完整回归已完成。
- 补充：2026-06-15 Android 主聊天已补 captive portal 网络预检和 AI 链接打开失败短提示 / 安全日志，只能降低门户 Wi-Fi、系统浏览器缺失或 ROM 链接拦截时的卡顿与无反馈风险；这仍不是完整极端网络验收。新包真机还需要覆盖飞行模式、无公网 Wi-Fi、弱网 / 断续网、门户 Wi-Fi、连续点发送 / 重试 / 清历史、长回复生成中切后台 / 锁屏 / 回前台、系统浏览器禁用或链接被系统拦截等场景，并结合 `auth.* / app.crash / ui.link_open_failed / ui.chat_startup_*` 日志看是否还有闪退、UI 回退或静默失败。
- 补充：2026-06-15 总门禁继续拆细为业务负责人更容易看懂的步骤：短信发送统计已从资源巡检中拆成独立 `sms usage`，并补了阿里云接口状态校验、默认不按签名过滤、空统计短重试和成功 / 失败 / 无回执汇总；空统计会输出 `sms_usage_status=attention`，避免误读成余额或验证码链路已无风险。后端数据边界巡检也从“非 `acct_...` 资产归属”扩展到 `acct_...` 孤儿记录和缺加密手机号账号检查。本轮线上只读复查这些新增检查均为 0；但这仍不替代 App 公安备案、后台登录后 smoke 实跑、短信套餐包余额确认、首封告警邮件送达和真机完整回归。
- 补充：2026-06-15 短信巡检继续补费用侧可见性：`check-sms-usage.ps1` 会调用费用中心 `QueryResourcePackageInstances` 查询当前有效资源包，并按商品码、包类型、备注和适用产品识别是否像短信套餐包；当前实测只返回百炼推理资源包和 OSS 存储包，没有返回短信类套餐包，因此脚本输出 `sms_package_status=not_visible_manual_required`。这只能证明费用中心 API 当前未暴露短信套餐包，不等于短信余额不足或充足；普通短信套餐包余额、到期、余量预警和自动复购仍需登录短信服务控制台人工确认，上线人工项继续保留。
- 补充：2026-06-15 总门禁新增“人工上线确认项”，默认把 App 公安备案、AccessKey 轮换、最终 APK 真机完整回归、SLS 首封告警邮件、短信套餐包余额和最终 release 物料一致性列在 `manual go-live checklist` 里；App 备案已通过并不再进入人工确认项。未通过临时环境变量显式确认时会计入 attention。它只把人工风险显性化，不把这些事项自动判成已完成；纯技术日常巡检可用 `-SkipManualGoLiveChecklist`，正式上线门禁不要跳过。
- 补充：2026-06-15 管理后台监控面板也同步区分人工确认和程序阻塞：`launch_readiness` 新增 `manual` 标记，首屏“上线人工确认项”只列显式人工项，避免把服务健康、App 错误或登录异常误写成“人工确认”。支付未接入在购买入口关闭、开发期订单端点关闭时只作为 attention，不阻塞免费版、礼品卡内测或无内购上架；但开放真实收费前仍是 R12 的硬风险，必须完成支付申请、验签、回调、对账和权益发放闭环。
- 补充：2026-06-15 管理后台 `launch_readiness` 人工项新增可选 `confirm_hint`，前端会在“上线人工确认项”和“正式上架检查”里显示“确认方式”，把 App 公安备案、AccessKey 轮换、最终真机回归、短信套餐余额、最终 release 物料和首封告警邮件分别说明去哪里确认、看什么证据、哪些敏感材料不能写进仓库或后台备注；后端单测和 `check-admin-surface.mjs` 会锁住人工项必须有确认方式。这只是降低负责人误读风险，不代表这些人工事项已经完成。
- 补充：2026-06-15 管理后台 `launch_readiness` 也新增“费用 / 套餐成本”人工确认项，把费用中心总账巡检的后续动作放进监控面板首屏和正式上架检查：确认账户余额、短信套餐余量、qwen-plus 资源包和百炼节省计划；DYPNS / 融合认证已按已购沉没成本处理，新 Android 不再使用，后续只确认不自动续费、不新增购买。后台只展示确认事项，不实时读取阿里云费用中心，不持有阿里云密钥，也不保存账单敏感截图、AccessKey 或密钥。
- 补充：2026-06-15 继续补“别被假绿误导”的只读门禁和监控表达：总门禁会把 `sms_package_status` 非 confirmed 计入 `sms usage and balance` attention，支付未接入但购买入口关闭时显示为 `payment closed guard` attention，不再把“支付 readiness ready”误读成真实支付已接入；公网黑盒新增后台首页 DOM marker 和首个 JS 资产 200 检查，避免后台默认页 / 静态资产丢失时只看 200。监控首屏也会在只有上线 attention、没有服务故障时说明还有多少程序项和人工确认项需要跟进，不再落到“暂时没有明显异常”。
- 补充：SLS 不是免费无限日志池，官方计费会涉及读写流量、索引流量、热存储、扫描查询、Shard 和告警短信 / 电话等。当前项目为了控成本只采 Go JSON 日志和 Nginx error log、TTL 7 天、1 shard、邮件告警、不采完整 Nginx access，App 自动日志也有 8KiB body 上限、脱敏和限流；2026-06-15 已新增 `check-sls-cost-guard.ps1` 并接入 `check-resource-capacity.ps1`，低成本护栏会检查业务 Logstore 数量、TTL、Shard、自动分裂、append meta 和归档存储是否超出当前预算口径。当前实测为 `server-go / nginx-error` 两个 Logstore，均 7 天、1 shard、未开启自动分裂或归档，属于首版低成本排障配置；后续用户量上来前仍需接入每天写入量 / 索引量 / 存储量 / 账单阈值巡检，必要时做采样、缩短保留期或购买 SLS 资源包。
- 补充：2026-06-15 已新增 `scripts/check-aliyun-costs.ps1` 做费用中心总账巡检，当前月账单 attention 主要来自百炼集中测试、短信套餐包和 DYPNS / 融合认证套餐。资源本身仍很低成本，ECS / RDS / Redis / OSS / 域名 / 短信套餐暂不建议升配；百炼最近 5 天税前约 `0.0714` 元 / 天，且 qwen-plus 推理资源包仍剩约 `11.49M / 12M tokens`，可等真实用户量起来再买资源包或节省计划。DYPNS / 融合认证订阅仍存在，但新 Android 已不再使用；当前两个融合认证包均为 `ManualRenewal`，不是自动续费，CLI 安全退订询价 `InquiryPriceRefundInstance` 对两个包均返回 `CommodityNotSupported`，因此按已购沉没成本记录，不把退款 / 退订作为上线前必须动作。后续只防自动续费或新增购买，不能由脚本自动做高风险财务动作。费用脚本的 `attention` 是经营提醒，不代表生产服务不可用。
- 补充：2026-06-15 按图片留存成本继续收紧 OSS 护栏：线上 Bucket 当前仍是 `uploads/` 问诊图 3 天删除、`support/` 反馈图 30 天删除、未完成分片 1 天清理；`check-resource-capacity.ps1` 已改为解析生命周期 XML，并确认前缀、启用状态、过期天数和分片清理都在对应规则内匹配，避免旧文本包含式检查把不同规则拼成假绿。图片删除后不再占 OSS 标准存储容量，但删除前仍可能产生存储、请求、生命周期处理和下载流量成本；当前对象量、100GB 标准存储包和压图 / 短生命周期策略足够早期使用，暂不需要升配。
- 补充：2026-06-15 上线总门禁已把费用中心总账巡检接入云资源段，显示为独立的 `aliyun costs` 步骤；非 ready 时进入 attention，用来提醒账单、套餐和模型资源包需要复核。这仍不是服务故障，也不自动购买、退款、续费、取消套餐或释放资源；正式 `-ReleaseGate` 前需要由业务负责人确认短信套餐包余额、模型资源包 / 节省计划策略，以及 DYPNS / 融合认证不再使用且不自动续费 / 不新增购买。
- 补充：2026-06-13 检查更新发布口径已收成普通更新：后台发布页不再暴露强制更新勾选，普通发版的更新说明可留空，后端和 App 统一展示“修复已知问题，优化使用体验。”；底层 `force_update` 只作为历史 / 兼容字段保留，不作为默认发版操作。这个收口降低误开强更和反复写更新文案的运营风险，但检查更新完整闭环仍未关闭：正式 APK 上传入口、完整回滚入口、旧包真机覆盖安装、安装未知应用授权和系统安装页仍要在真实发版前验收。
- 补充：2026-06-12 发布 / 回滚脚本已同步验证 API 与后台 `/admin-api/` 上游跟随同一个 slot，并在回滚失败时恢复 Nginx 配置；2026-06-13 发布脚本也补齐切换后后台 `/admin-api/` 上游端口显式校验，不再只靠未登录 401 判断后台健康，降低后台仍指旧 slot 却被误判成功的风险。这里剩余的“发布 / 回滚写操作”主要指尚未做成管理后台页面的一键发版 / 回滚入口。
- 后续动作：保留 `check-sls-alert-readiness.ps1 -RequireExternalNotification -RequireDashboard -FailOnWarning` 和 `check-public-blackbox.ps1` 作为巡检门槛，并在真实或测试告警触发时确认邮件送达；用 owner 凭据执行 `check-admin-authenticated-smoke.ps1 -RequireOwner` 完成一次登录后核心 API smoke；继续补数据库只读脚本、后台账号受控重置 / 禁用 / 角色管理，把真实发版、回滚、查日志、查库、客服回复、礼品卡或会员运营入口沉淀到 runbook，并按最小权限继续拆角色。
- 性能补充：2026-06-12 后台总览 / 监控 / 产品洞察聚合接口已加 4 秒查询超时，且补了 App 日志、有效 session、待回复反馈相关索引，能降低后台刷新扫表和慢查询拖住请求的风险；但这只是后台读路径优化，不等于完成独立黑盒监控、自动自愈或真实高并发压测。
- 性能补充：同日继续新增账号新增、会员到期、当日额度、扣次、加油包和升级补偿相关索引，降低后台聚合在真实用户量增长后扫表的风险；客服正文关键词洞察这类 `%关键词%` 模糊扫描仍是后续观察项，不能靠普通索引完全解决。
- 性能补充：2026-06-13 继续给订单按账号ID查询和礼品卡兑换失败原因聚合补索引，降低后台订单核查、礼品卡异常统计在数据增长后拖慢监控页的风险；这仍只是后台读路径优化，不改变支付未接入、礼品卡发放对象管理未完成和首封告警邮件待确认等风险状态。
- 性能补充：2026-06-16 后台用户、会员汇总、订单、用户详情、帮助反馈、App 日志、审计、今日农情和检查更新等只读接口已补统一 4 秒查询超时；公网黑盒也新增一组未登录后台保护接口探针，避免路由暴露或鉴权漂移只靠首页 200 被误判。剩余风险仍是 owner 凭据登录后的核心 API smoke、自动定时黑盒通知、更细趋势告警和真实数据量增长后的慢查询观察。
- 补充：2026-06-16 readiness 门禁继续收紧后台生产 Cookie 安全口径：`ADMIN_COOKIE_SECURE=false/no/off/0` 会直接失败；非 `prod/production` 环境若未显式开启 `ADMIN_COOKIE_SECURE=true` 也会失败，避免后台登录 Cookie 在生产 HTTPS 入口被配置漂移成非 Secure。管理后台列表类接口和礼品卡追溯接口已继续使用短超时和通用错误码，避免慢查询拖住连接或把内部错误细节直接暴露给浏览器；剩余风险仍是 owner 登录后 smoke、自动定时黑盒通知和真实数据量增长后的慢查询观察。
- 补充：2026-06-17 Web CI 曾因 Vite 8 / Rolldown optional peer 依赖在 GitHub Linux `npm ci` 阶段解析更严格而失败，表现为缺 `@emnapi/*` 条目；当前已在 `admin/package.json`、`site/package.json` 显式声明 `@emnapi/core` 和 `@emnapi/runtime` 为开发期构建依赖，并刷新两份 lockfile，本机已分别跑通 `npm ci` 与 `npm run build`。这属于供应链构建依赖一致性修复，不改变后台功能、官网首页文案或线上部署状态；后续若再升级前端构建依赖，必须优先跑 `npm ci`，不能只跑 `npm run build`。

## R2 项目记忆已有程序化检查，但覆盖仍偏粗

- 状态：未关闭
- 说明：仓库里已有 `scripts/check_project_memory.py`，CI 也会在关键真相文件变更时检查是否同步更新了项目记忆文件；2026-06-12 已从“至少改一份 memory 文件”收紧为按变更类型要求特定文件：关键代码 / runbook / 运维脚本变更默认要更新 `recent-changes.md`，当前真相类变更要更新 `current-status.md`，登录 / 部署 / 监控 / 支付 / 礼品卡 / 今日农情等风险敏感区域还要更新 `open-risks.md`，项目记忆校验策略本身变化还要同步 `pending-decisions.md`。2026-06-17 已新增独立 `project-memory.yml` 专门在 GitHub 跑项目记忆检查，并把 `admin/**`、`site/**`、`.github/workflows/**`、测试包清理 / 发布脚本、正式包校验脚本、上线总门禁脚本纳入 watched/current-status 覆盖；同日新增 `web-ci.yml`，后台和官网源码改动会跑对应 Vite / TypeScript build
- 风险：当前仍是文件级校验，不会判断具体章节内容是否写对，也不能自动识别所有业务语义；即使 CI 通过，记忆文件之间仍可能出现轻微漂移或局部过期，影响新窗口对“当前真相 / 已关闭风险 / 仍待决策”的判断
- 补充：2026-06-15 曾巡检发现根 `AGENTS.md` 的 ECS active slot 口径滞后于实时 `check-ecs-readiness.ps1` 输出；2026-06-17 只读巡检再次提示 active slot 这类动态事实不适合在长期规则里写死。该例子说明项目记忆文件级校验仍需要配合实际脚本输出和人工巡检，不能只看“记忆文件被改过”就默认语义全对；当前 active slot 以 `scripts/check-ecs-readiness.ps1` 实时输出为准，文档只记录最新观察和操作口径，不把 `3000` 或 `3001` 当长期常量。
- 后续动作：观察误报率和维护成本；如果仍出现文档互相打架，再把校验继续细化到“特定章节 / 特定关键词 / active slot 口径”等更强一致性检查

## R3 GitHub 协作层尚未结构化

- 状态：未关闭
- 说明：目前仓库已有 CI，但还未补充 Issue 模板、PR 模板、CODEOWNERS、项目字段等协作骨架
- 风险：跨窗口协作能接住仓库内真相，但任务排队、责任归属、变更说明仍不够结构化
- 后续动作：需要时再按最小改动补 GitHub 协作层

## R4 聊天运行时正向列表主链已基本稳定，仍保留边角观察

- 状态：未关闭
- 说明：当前聊天消息运行时已按用户拍板从反向列表切回单一正向 `LazyColumn` 主人：`ChatRecyclerViewHost.kt` 不再使用 `reverseLayout` / `items.asReversed()`，`messages` 仍按 oldest -> newest 直接显示，最新消息在 `lastIndex`。mixed active-zone / overlay、小分割 list itemization、streaming `scrollBy` / `dispatchRawDelta` 高度补偿都不在主链运行
- 已验证：2026-04-28 用户真机反馈当前滚动链“确实很稳”。核心链路包括：`SideEffect` 同帧底部锚定压 streaming 下一行冒头闪；用户滑回正向列表物理底部后快速恢复 AutoFollow；96dp 工作线以下空白按当前贴底链露出；上滑 / 下滑整体不再抢手
- 剩余风险：仍需在更长回复、不同输入法 / 设备、含免责声明答案、冷启动首次点输入框等边角场景继续观察；如果再次出现冒头闪、尾部收口微动或手动回底不跟随，下一刀优先检查当前正向列表底部锚点、reveal 提交节奏、工作线 bounds、contentPadding 与 item padding 的几何关系，不恢复 overlay、小分割或 raw delta
- 风险补充：2026-06-15 主界面深度交互静态巡检未发现 `reverseLayout / asReversed / dispatchRawDelta / scrollBy / SparseBottomSpacer / BottomActiveZone / StreamingBlockChatListItem / StreamingTextBlock` 等旧链残留；`scripts/check-android-build-parity.ps1` 已把这些禁用符号、单个正向 `LazyColumn`、streaming 同帧底部锚定、今日农情普通列表项和 AI 链接可点状态纳入门禁，GitHub Android CI 也新增 `:app:testDebugUnitTest`。这能防止代码回潮，但本轮 `adb devices` 未发现已连接真机，清数据首屏、IME、连续发送、今日农情可见性、图片预览、链接点击和额度提示仍需下一次装包后做真实设备回归。
- 风险补充：2026-06-16 继续复查链接交互后，AI 回复完成态含 Markdown 链接或裸 URL 时已重新保留 `SelectionContainer`，同时继续使用 URL annotation 和系统 URI handler 打开链接；`check-android-build-parity.ps1` 已把“链接可点 + 复制 / 全复制可用”改成门禁。仍需真机长按含链接回复、短点链接、链接打开失败复制兜底和复制菜单遮挡做设备回归。
- 风险补充：pending finalize 仍不主动 bottom align，主要依赖两阶段 fresh bounds、unified soft-wrap renderer 和 streaming 免责声明几何占位；极端切后台 / 布局回调丢失 / bottom reserve 迟迟不 ready 时有约 1500ms 绝对超时兜底，避免一直停在生成态，超时路径不会强制底部锚点。若含免责声明答案收口仍微跳，优先在同一消息主人内复核高度来源，不恢复完整 `scrollToBottom(false)` 精修
- 风险补充：composer 当前已完成 P0 拆链、取消发送旧高度锁、统一收键盘路径为 `clearFocus(force = true)`，并删除已经没有 true 入口的 `ChatComposerCollapseOverlay` / `composerCollapseOverlay*` 旧链。若真机仍有输入框残影或冷启动首点迟钝，下一步才评估是否需要 `WindowInsetsAnimationCompat`；不要把 IME padding 挪到根容器去抬升消息列表，也不要动滚动主链
- 风险补充：极端恢复链已做低风险兜底：断网连续发送同文复用已有失败用户消息；远端 streaming 重启恢复失败会补 assistant 重试入口；切后台会清消息 / 输入选择菜单；`SessionApi` 保留 active SSE call 引用直到读循环退出，方便 reset / cancel。2026-06-13 主聊天本地联网预检已放宽为只看系统互联网能力，不再因 VPN / 代理 / ROM 特殊网络没有 WiFi、蜂窝或以太网 transport 就提前判离线；SSE 正常 DONE 但最终内容为空时也会保留 assistant 失败态和重试入口，不再静默删占位或标记后台任务完成。剩余观察点是：后台 `PendingChatSendWorker` 流式读取若被系统停止，仍可能受无读超时读循环影响；前台图片流失败后取消 pending 任务的边界仍需继续观察；额度用完前端锁当前是本地会话级，App 被系统杀掉后仍以第一次后端 quota 返回为准；图片入口首版已接入相机 / 照片、后台下采样预览、压缩上传和 URL 恢复显示，相机 FileProvider URI 已补读写 flags、ClipData 和显式包授权，但仍需继续真机验证第三方相机 / 相册 URI、弱网多图上传失败、后端图片生命周期和 OSS 权限
- 风险补充：2026-06-14 主界面短提示从输入框上方改为屏幕中部全局浮层，额度耗尽仍在消息流里保留 assistant 重试尾巴，但尾部文案复用“回复未完成 · 点击重试”，不再单独显示“今日额度已用完 · 点击重试”；今天点击只提示额度，第二天额度恢复后可沿用原重试链继续发同一条；限流和活动流进行中这类明确不可立即重试的中断只保留中部提示，避免和输入框、消息尾部或其它黑色提示重叠。跳到底部 / 静态底部锚点会把今日农情等视觉尾部纳入目标，减少只有卡片或卡片在尾部时的回底错位；回到底部按钮当前只在用户滚离底部后短暂显示约 2.2 秒，避免长期压住内容。若今日农情原锚点消息被 30 轮 UI 窗口裁剪，卡片会插在历史保留提示之后，降低首屏提示和农情卡顺序错乱风险。该改动只调整状态展示和视觉锚点，不恢复 overlay、反向列表或 raw delta；仍需真机继续观察输入法、今日农情卡片、长回复、额度耗尽和复制菜单同时出现时是否还有遮挡。
- 风险补充：2026-06-17 今日农情已从旧边框卡片和 card-only 顶部特例收成普通 AI 文本视觉项，且主聊天改为当天一次性展示：后端清晨生成，但没有真实聊天消息时只显示欢迎语，今日农情不能替代或压掉空态；只有安静打开且可见尾部是完整 AI 回答时，今日农情才跟在这条完成回答后方，失败 / 未完成 assistant 尾巴不算完整回答；当天已插入可见主聊天 timeline 后写入本地已展示日期，关闭重开不再反复出现；如果用户先开始发送且今日农情还没进可见列表，本次运行抑制自动插入，不在刚发送完消息时突然弹出。它仍是 `ChatTimelineItem.TodayAgriCard`，不写入真实 `messages`，不变成 overlay / sticky / 关闭动画；设置页“今日农情”历史入口仍可查看最近记录。`./gradlew.bat :app:compileDebugKotlin` 已通过；仍需用户安装新包后在清数据 / 无真实消息 / 后续发送新消息 / 重开 App 四个场景真机看一眼首屏到工作线、开机贴底、自然上移和当天不重复是否符合预期。
- 风险补充：2026-06-17 继续按冷启动全链路复查修正今日农情边界：`START` 锚点不再在重开 App 后迁移到最新真实消息；远端 snapshot 成功返回空历史时会清掉本地完成旧窗口；如果清空后只剩今日农情视觉项，启动工作线相位会重新按 `TopUnreached` 文档流处理；今日农情锚点保存新增 `remoteSnapshotHydrationComplete` 等待成功远端 snapshot，远端失败只本地兜底展示、不落盘新锚点，避免在远端历史替换前锚到本地旧尾巴。`check-android-build-parity.ps1` 已锁住这些护栏，并在 `ui.chat_startup_state` 日志里区分 `history_hydrated` 和 `remote_snapshot_hydrated`。这仍需要最新包真机复测清数据首次进、重开 App 先本地后远端、远端无历史但有今日农情三个场景，不能只凭静态检查宣布彻底根治。
- 风险补充：2026-06-17 普通 AI 回复的轻分割线已从只识别 `# / ##` 标题扩展到 `# / ## / ###` 和独立短加粗标题，避免近期模型输出 `**处理建议**` 时分割线看起来消失；同时单测和 parity 门禁锁住行内加粗正文不触发、连续标题不叠加多条线。该改动不改变主滚动链、不增加模型输出硬限制、不修改三份提示词；仍需用户安装新包后用真实长回复看一眼分割线密度是否舒适，若偏多，优先收窄标题识别边界，不恢复旧滚动方案。
- 风险补充：2026-06-15 按用户反馈小球跳动不明显，只调 Android 主聊天等待小球和流式吐字节奏：远端首个 chunk 太快返回时，小球至少展示约 1.8 秒，streaming reveal 收为 1 个 token 一拍，中文单字、普通 token、标点和换行停顿整体放慢。该改动不改变正向 `LazyColumn`、96dp 工作线、AutoFollow、今日农情列表项、失败 / 重试尾部或后端输出；仍需用户安装新包后真机看小球是否够明显、正文是否不拖沓。
- 风险补充：2026-06-15 按用户视频里“生成中文字有点往下掉又上来”的体感，`onAdvance` 已去掉写入新内容前的程序化底部锚定，改为依赖内容提交后的 `SideEffect` / `requestScrollToItem` 下一次 remeasure 锚定，避免同一批 reveal 前后两次锚点抢节奏。该改动仍保留边生成边渲染，不恢复 overlay、反向列表、小分割、raw delta 或 scrollBy；仍需真机长回复复测加粗、编号、表格、今日农情同屏和手动上滑 / 回底。
- 风险补充：2026-06-15 按用户视频继续深查 streaming 渲染和滚动：保留 `SideEffect` 同帧底部锚定，不恢复 overlay、反向列表、小分割或 raw delta；`ChatScrollCoordinator.kt` 已收窄 streaming 期间的滚动归因，避免把同帧锚定 / 内部 remeasure 造成的 `isScrollInProgress` 当成用户浏览而短暂停 AutoFollow。`ChatStreamingRenderer.kt` 新增 streaming-aware inline Markdown 子集，加粗、斜体、行内代码和链接在生成中尽量实时渲染，streaming 帧如果刚好停在待续的 `**`、`*` 或反引号也不再短暂露 raw marker，DONE 后按打字节奏 drain 本地 buffer，不再一口气 flush 尾段。当前 renderer 仍是轻量子集，不是全量 Markwon / CommonMark / GFM；复杂 Markdown 能力若要扩大，应另起受控评估，不在这条滚动修复里顺手替换渲染引擎。仍需用户用真机复测长回复、加粗 / 链接 / 表格样本、清数据首屏、今日农情卡片和手动上滑 / 回底。
- 风险补充：2026-06-15 今日农情视觉时间线插入已补单测，覆盖只有卡片、卡片锚在真实消息后、原锚点被 30 轮 UI 窗口裁掉后落到历史提示后；`SessionApi.resetUiRuntimeForCleanState()` 也不再清空共享 handler 的全部回调，只取消当前 SSE。该收口降低清空历史时误取消设置 / 会员 / 反馈 / 更新回调的风险，但仍需真机继续看清数据首屏、后续发送自然上移和长回复滚动稳定性。
- 风险补充：2026-06-15 深查清数据 / 清历史竞态后，发现 `SessionApi.getSnapshot()` 和 `getTodayAgriCard()` 在 runtime generation 过期时存在静默退出路径，可能让等待远端 hydrate 或今日农情加载的协程悬挂。当前已改为过期也回调空结果，并把该行为写入 `check-android-build-parity.ps1`；这不改变滚动链、不恢复旧方案。仍需用户安装新包后真机验证：系统清数据首次进 App、设置页默认入口、今日农情是否回到主列表、慢网 / 断网 / 连续发送时是否不再出现 UI 回退或卡住。
- 风险补充：2026-06-15 用户反馈同一旧包“设置里能看到今日农情，主界面文本区没有”；生产日志显示今日农情已生成，设置页历史接口和主界面 `/api/today-agri-card` 都返回 200，且未见 `today_agri.fetch_failed`。当前手机仍是 2026-06-14 21:31 左右安装的旧 debug 包，后续提交 `29284b01 Restore today agri card after remote hydrate` 和本轮新增的 `today_agri.main_card_loaded / today_agri.main_card_visible` 诊断尚未在真机复测；因此该项仍保持观察，下一次应先安装最新 debug 包，再看主界面卡片是否出现，并用后台 App 日志确认是“已加载 / 已插入 / 视觉不可见”哪一层。
- 后续动作：把当前正向列表滚动链作为稳定基线保留；短期只做边角验证和低风险清理，不再主动重构滚动主链

## R5 聊天页主文件偏重但暂不影响运行时主链

- 状态：未关闭
- 说明：`ChatScreen.kt` 当前承担聊天列表装配、滚动几何、发送 / 恢复、图片导入 / 重试、会员入口接线和 debug-only 文案预览等多类职责，文件仍偏长，后续维护成本高。当前已排查：会员中心 UI 本体在 `MembershipCenterSheet.kt`，图片全屏预览在 `ImagePreviewPager.kt`，今日农情卡片渲染已拆到 `TodayAgriCardUi.kt`，聊天区用户图片 strip / 过期占位 / 预览关闭按钮已拆到 `UserMessageImageUi.kt`，图片预览下采样和 LRU 缓存已拆到 `ChatImagePreview.kt`，输入框无入口的 `ChatComposerCollapseOverlay` 旧链已删除，都没有并入滚动主链；旧 active-zone / overlay / raw delta 方案未在运行时残留
- 风险：这属于可维护性风险，不等于用户端一定卡顿。真正运行时风险仍来自高频测量、Selection、Markdown、图片缩略图解码和滚动锚点状态；若为了“瘦身”贸然拆滚动几何或发送事务，反而可能把已稳定的工作线 / AutoFollow 打坏
- 后续动作：短期不拆 debug-only 文案预览面板，不动滚动主链。后续若要继续瘦身，优先搬消息菜单、debug 预览 UI 等无状态 / 低耦合片段；不要第一刀拆 `commitSendMessage`、两阶段 finalize、滚动 coordinator 闭包或 `ChatRecyclerViewHost`。`scripts/check-android-build-parity.ps1` 已开始拦截今日农情 UI、用户图片 strip、图片预览解码缓存重新塞回 `ChatScreen.kt`，以及 `ChatComposerCollapseOverlay` / `composerCollapseOverlay*` 旧链回潮。

## R6 外部会诊仍依赖人工转发上下文

- 状态：未关闭
- 说明：小米 / MiMo 免费版、Claude 等外部模型默认看不到本地仓库，只能依赖用户通过聊天软件转发的代码片段、日志和截图；即使仓库内规则已收紧，会诊结果仍受转发上下文完整度影响。当前用户偏好已调整为：本项目后续外部会诊默认优先整理给小米 / MiMo
- 风险：如果外发内容不自包含，对方仍可能脑补仓库结构、假设不存在的接口，导致方案听起来合理但无法直接落地
- 后续动作：继续坚持“问题说明 + 关键代码片段 + 明确追问 + 已排除项 + 限制条件”的短稿格式；尤其发给小米 / MiMo 时要把当前真实代码结构、关键函数 / 状态和不能碰的旧方案写清楚。收到方案后先由 Codex 对照当前代码核验再下刀

## R7 真实后端已部署，公网正式上线条件仍未闭环

- 状态：未关闭
- 说明：首版部署路线已从“SAE 镜像托管优先”转向“ECS 传统部署优先”。ECS `i-2ze5nrem0jrchln4f0eh` 已购买并运行，位于 `华北2（北京）/ cn-beijing` 可用区 L，规格 `ecs.u1-c1m2.large`（2 vCPU / 4 GiB），Ubuntu 22.04 64 位，公网 IP `39.106.1.151`，私网 IP `192.168.1.237`，生产 VPC `nongjiqiancha-prod-vpc` / `vpc-2zeax2zowza2398b9dzot`，生产交换机 `nongjiqiancha-prod-beijing-l` / `vsw-2zemsq82lj2kp8za90aky`，安全组 `sg-2ze4tilwxw1h5w77lwl1`，固定公网带宽 5 Mbps，CLI 当前显示到期时间 `2027-06-01T16:00Z`；2026-05-30 已清理北京区空闲默认 VPC / 默认交换机和旧 SAE 自动交换机，当前只保留生产 VPC / 交换机。已初始化系统用户、部署目录、systemd、Nginx 反向代理、基础限流、fail2ban 和 logrotate，`server-go` 已部署并运行。Go 服务已收口为只监听本机回环地址，当前用 `nongji-server-3000.service` / `nongji-server-3001.service` 双端口 slot 发版，由 Nginx 在 `127.0.0.1:3000/3001` 之间切换，公网仍统一只走 Nginx；通用 JSON body 默认只读取 64KiB，App 日志接口单独限制 8KiB 且超限返回 413；模型出站请求已限制拨号、TLS 握手、响应头等待和空闲连接，SSE 响应已带 `X-Accel-Buffering: no`，主聊天流有 30 分钟最长持续时间兜底，记忆文档摘要非流式模型响应限制 64KiB，今日农情 JSON 响应限制 1MiB；服务启动迁移已加 MySQL 全局命名锁，迁移锁释放失败会作为启动错误暴露。阿里云 DNS 已创建 `api`、`@`、`www` A 记录指向 `39.106.1.151`；2026-06-05 截图确认网站 ICP 备案已通过，主体备案号 `京ICP备2026031728号`，网站备案号 `京ICP备2026031728号-1`；2026-06-05 已通过 Let’s Encrypt / certbot 为 `api.nongjiqiancha.cn` 配置 Nginx 443 HTTPS，公网 HTTPS healthz 返回 200；2026-06-06 已为根域名官网 `nongjiqiancha.cn` / `www.nongjiqiancha.cn` 配置 Nginx 静态站和 HTTPS，公网返回 200。App 备案已通过，备案号为 `京ICP备2026031728号-2A`，Android 设置页底部和协议 / 隐私基础信息只展示该编号，不再单独放“备案查询”行；2026-06-10 网站公安联网备案号已下发：`京公网安备11010602202723号`，App 公安备案待补；公安备案数据码不进入仓库；同日已通过 Cloud Assistant 将 DashScope 主 / 副模型 Key 写入 ECS 主备槽位并重启服务，`/healthz` 显示 `bailian=ok`，真实 Key 值不进入仓库。RDS MySQL 实例 `rm-2zes3vmj76p85n8g1` 已创建数据库 `nongjiqiancha`、应用账号 `nongji_app`，白名单为 `192.168.1.237`，服务启动已完成迁移。旧 SAE demo 应用已删除，SAE `ListApplications` 为空。域名 `nongjiqiancha.cn` 已购买，用户口头确认实名认证 / 模板审核已通过，网站 ICP 和 App 备案均已通过；网站公安备案已通过，App 公安备案和真机生产 API 回归仍未完成。OSS 标准-本地冗余存储包（华北2）100GB 已购买并生效，Bucket `nongjiqiancha-prod` 已在北京创建为私有标准本地冗余，并配置 `uploads/` 3 天、`support/` 30 天生命周期；`server-go` 已新增 OSS 上传存储后端，生产 ECS 已配置 OSS 环境变量并通过 `upload_storage=oss` 验证，当前会写私有 OSS。SLS 服务本体已开通且农技千查专用 Project / Logstore 已接入最小排障集：Project `nongjiqiancha-prod-1159547719787456`，Logstore `server-go` / `nginx-error`，TTL 7 天、1 shard，ECS Logtail 采集 `/var/log/nongjiqiancha/server.log` 和 `/var/log/nginx/error.log`；当前未购买 SLS 节省计划 / 资源包，已创建 5 条 AlertHub 最小告警，并已绑定邮件行动策略和最小仪表盘。北京区仍有 3 个阿里云系统 / 产品托管 Project，2026-05-30 已复查为云监控 / XTrace / 产品数据类，不直接删除。Redis 开源版实例 `nongjiqiancha-prod-redis` / `r-2zet46zvmoo9wu3bic` 已购买并运行，256MB、Redis 7.0、标准高可用主备、同生产 VPC / 北京可用区 L，白名单已限制到 ECS 私网 IP；`server-go` 已新增可选 Redis 客户端和短期限流 / 临时认证状态，当前主聊天用户级频控已可走 Redis，但主聊天内容、额度、归档、摘要和订单仍不依赖 Redis；PolarDB 暂作为后续高规格升级选项
- 风险：后端已能运行但还不是完整生产可用：模型 Key 主 / 副槽位已配置且 `/healthz bailian=ok`，但主聊天、图片问诊、今日农情还需要用真实 App token / 真机链路回归，仍可能暴露模型权限、额度、联网搜索、模型拉图或限流问题；`AUTH_STRICT=true` 已保护公网业务入口，手机号账号 / per-user token 骨架已落地，短信签名和验证码模板已写入生产环境，`/healthz` 已显示 `sms=ok / redis=ok`，Android 新包已改为自有普通短信验证码登录页，但真机短信验证码登录完整回归仍未闭环，已暴露过的主账号 AccessKey 上线前必须轮换；Redis 认证限流已部署到 ECS 并通过 `/healthz redis=ok` 验证；网站 ICP、App 备案、根域名官网和 API HTTPS 已完成，网站公安备案号已下发并已补官网 footer，App 公安备案待补，App 生产 API 地址仍需真机回归后再稳定切过去；OSS Bucket、代码后端和生产 ECS OSS 环境变量均已准备好，当前线上上传后端已切到 OSS；后续加第二台 ECS 或回到 SAE 多实例前仍需用真实 App 链路验证 OSS 上传 / 读取 / 模型拉图；多实例前摘要仍需要跨实例保护；当前双端口发布只降低单机重启空窗和流式连接被立即踢掉的概率，不等于多机高可用或数据库迁移可自动回滚；公网 SSH 22 端口已撤销安全组放行且 ECS 本机 `ssh` 服务已停用，生产运维优先走阿里云 CLI + Cloud Assistant，若后续临时开 SSH 必须限定固定来源 IP 并用完关闭
- 补充：2026-06-15 用户截图确认 App 备案管局审核已通过，备案号 `京ICP备2026031728号-2A`；阿里云页面提示公安备案需登录全国互联网安全管理服务平台单独提交，按网站 / App 开通后 30 日内处理。公安备案不是工信部 ICP / App 备案，也不是继续在阿里云备案订单里提交；数据码、证件号和账号信息仍不进入仓库。
- 补充：后端已支持 `DASHSCOPE_API_KEY_1/2/3` 主备 Key 池和限流前置切 Key，`DASHSCOPE_API_KEY_1` 健康时优先作为主 Key 使用，`DASHSCOPE_API_KEY_2` 可作为副 Key；但真实并发扩容必须使用不同阿里云主账号的 Key，同一主账号多个 API Key 共享 RPM / TPM 限流。朋友账号 Key 可短期兜底，但长期生产会带来账单、权限、密钥轮换和数据处理责任不在自己名下的运维风险
- 补充：2026-06-14 按用户最新口径，生产 Key 池从 `auto` 改为 `fallback`：主 Key 优先吃满，副 Key 只在主 Key 开流前限流 / 额度 / 鉴权类失败时同次请求兜底，不再按请求数或 token 阈值提前轮询。`auto` 的请求数 + token 双信号能力仍保留为后续高峰可选方案，但当前生产不启用；上线后仍要观察真实 `model_total_tokens`、429 / quota 日志、主 Key 是否频繁撞限、以及副 Key 兜底比例，再决定是否需要恢复主动轮询或升级资源。
- 补充：2026-06-06 资源巡检确认当前 ECS / RDS / Redis / OSS 规格和用量都很宽裕，暂不需要立刻升配；同日发现一次 502 的根因是双端口发布的多个 `nongji-drain-stop-*` transient 排空任务叠加，而不是资源不足。已恢复 active slot，并把部署 / 回滚脚本改为发布或回滚前先清旧 drain 任务；readiness 脚本已改为 active slot inactive 或 Host healthz 非 200 时直接失败。Go 后端已补请求级日志和 ECS / SLS 查询脚本，SLS 当前已接入最小排障采集和 5 条 AlertHub 最小告警；2026-06-12 又补阿里云云监控邮件联系人组和 9 条 ECS / RDS / Redis 资源水位告警，并给 ECS 系统盘绑定普通低频自动快照策略。剩余风险是首封 SLS 告警邮件送达尚未通过真实 / 测试触发确认，以及缺少自动自愈，用户仍可能比系统更早发现部分业务异常
- 补充：2026-06-12 readiness 脚本继续收紧，远端 `nginx -t` 配置检测失败会直接让检查失败，不再只打印后继续跑后续项；这能防住反代配置坏掉时的“假绿”。同日新增 `scripts/check-resource-capacity.ps1` 和 `scripts/check-backend-data-boundaries.ps1`：资源巡检显示 ECS / RDS / Redis / OSS 容量宽裕，OSS 已开启默认服务端 AES256 加密；ECS / RDS 是包年包月，删除保护接口不适用，Redis 释放保护已开启；ECS 系统盘已绑定普通低频自动快照策略，每周二、周六北京时间 04:00 创建并保留 7 天；资源巡检现在会深度校验云监控资源、阈值和周期，严格模式会把 SLS 外部通知 / 仪表盘缺失当失败，当前已通过；数据边界巡检显示当前没有正式用户、没有会员 / 订单 / 礼品卡资产，且所有需要账号归属的表非 `acct_...` 计数均为 0，并会输出 24 小时 App warn / error Top 事件辅助登录排障。剩余风险仍是真机登录 / 主聊天 / 图片问诊回归、App 公安备案、AccessKey 轮换、首封 SLS 告警邮件送达确认、公网黑盒自动定时通知和帮助反馈记录保存 / 删除 / 注销合规口径。
- 补充：2026-06-15 按阿里云官方文档复核释放保护适用范围后，`check-resource-capacity.ps1` 已把 ECS / RDS 包年包月实例的释放保护输出改为 `deletion_protection=not_applicable_prepaid`，避免把旧的 `False` 误读成缺少保护；只有 ECS 按量付费或 RDS 按量付费 / Serverless 且未开释放保护时才会给 warning。当前资源严格巡检仍为 `warnings=0 / errors=0 / status=ready`。
- 补充：同日发布 / 回滚脚本补齐后台 `/admin-api/` 上游同步切换和 401 验证，且生产 health 断言加入 `sms=ok`；严格资源巡检会把 SLS warning 漂移也当失败。剩余风险仍是真机登录 / 主聊天 / 图片问诊回归、App 公安备案、AccessKey 轮换、首封 SLS 告警邮件送达确认、公网黑盒自动定时通知和帮助反馈记录保存 / 删除 / 注销合规口径。
- 补充：2026-06-13 已把 App 各层限流和大小 / 超时边界整理到 [app-traffic-limits.md](D:/wuhao/docs/runbooks/app-traffic-limits.md)，并复查 / 调整线上 Nginx 配置。当前没有全局 App 用户数 / DAU / 主聊天总并发硬卡；主聊天流 Nginx IP 级限制已从旧的 `6r/m`、burst 3、单 IP 2 连接调宽为 `60r/m`、burst 80，并移除聊天 `limit_conn`，避免多人共用 WiFi / NAT 时被误伤。若真机回归仍出现 429，先区分是 Nginx、Go 用户级 `20/60s`、会员次数、短信 / 认证、上传还是模型供应商限流，不新增全局主聊天并发硬闸，不通过模型输出上限解决流量问题。
- 补充：2026-06-06 免费优先安全加固已执行：阿里云云安全中心免费版显示 ECS agent online、当前风险计数 0；阿里云 DDoS 基础防护按官方口径默认免费开启；Nginx 全局安全头兜底已补，官网部署脚本会保留 CSP / HSTS / Permissions-Policy；API HTTP 80 已改成 ACME challenge + 301 HTTPS 跳转；后端内部 secret 入口已补 IP / Redis 频控，主聊天 `client_msg_id` 已补 128 长度上限。当前不先买付费 WAF、云防火墙或 DDoS 高防，后续如果出现持续 Web 攻击、CC、基础 DDoS 防护触发黑洞或运营成本异常，再按 [security-hardening.md](D:/wuhao/docs/runbooks/security-hardening.md) 和 [security-threat-model.md](D:/wuhao/docs/runbooks/security-threat-model.md) 升级
- 补充：2026-06-14 阿里云云安全中心只读巡检看到 1 条提醒级 `云产品威胁检测-OSS可疑访问行为`，关联 OSS 读取 Bucket 加密配置类操作；核对后与本次 CLI / ossutil 做 OSS 配置巡检或开启服务端加密的运维行为一致，已按“我已手工处理”收口，未创建长期白名单，待处理列表为空。仓库和聊天不记录 AK、IP、UserAgent 等敏感字段；后续若出现来源不明、写操作、删除操作、失败调用或越权类告警，不能静默忽略，应按安全事件处理。上线前仍继续按最小权限 RAM 用户和主账号 AccessKey 轮换口径收口。
- 补充：2026-06-16 继续复查服务器安全、性能和未来升级准备：ECS readiness、公网黑盒、安全加固脚本、资源容量严格巡检、后台数据边界巡检均通过；公网入站只剩 80 / 443 / ICMP，SSH 关闭，Go 只监听本机回环 3000/3001 slot，近期没有业务 5xx / 429，只有普通公网扫描 404 和少量 TLS 握手噪声。当前不升配、不买 WAF / 高防；后续按指标触发升级：资源水位高先原地升 ECS / 带宽，单机不够再 SLB + 多 ECS，数据库压力再升 RDS / 只读实例，持续 Web 攻击 / CC 才评估 WAF，DDoS 黑洞或带宽打满才评估高防。记忆文档摘要已补 Redis TTL 租约降低多实例重复摘要风险；多 ECS 前仍必须补滚动发布脚本、跨实例日志采集、SLB 后真实 IP 链路复核，并灰度观察 Redis 租约与 `pending_retry_b` 重试。
- 补充：2026-06-16 供应链安全复查发现本机 Go 1.26.2 标准库和旧 `golang.org/x/net` 有已修复漏洞命中调用链；已把 `server-go/go.mod` 钉到 `toolchain go1.26.4`，并把 `golang.org/x/net` 升到 `v0.53.0`，复查 `govulncheck ./...` 显示当前代码实际调用链 0 漏洞。该项已修复，但后续每次 Go 安全补丁发布或依赖升级仍应跑 `govulncheck`；上线前更高优先级风险仍是主账号 AccessKey 最小权限轮换、App 公安备案、最终真机回归和首封 SLS 告警邮件确认。
- 补充：2026-06-16 后端容器构建基线同步到 `golang:1.26.4-alpine`，GitHub server CI 新增 `govulncheck ./...`，避免本机 go.mod/toolchain 已升级但 Dockerfile 或 CI 仍落在旧补丁版本。该项只提高供应链门禁，不代表生产 ECS 已因本轮提交重新部署；下一次后端部署仍必须按 readiness / 黑盒 / 真机回归节奏验证。
- 补充：2026-06-16 最新后端与后台已部署到 ECS，active upstream 切到 `3001`，后台 `/admin-api/` 同步跟随 `3001`，readiness 和公网黑盒均通过；旧 `3000` slot 处于排空 / 回滚窗口。记忆文档摘要已补 Redis TTL 租约，但多 ECS 前仍需补滚动发布脚本、跨实例日志采集和 SLB 后真实 IP 链路复核，并在灰度中确认 Redis 租约失败时只保留 pending 重试、不重复打摘要模型。
- 补充：2026-06-16 继续补前端构建供应链安全：`admin` 和 `site` 的 Vite / esbuild audit 高危告警已通过升级 Vite 到 `8.0.16` 清零，两个前端 `npm run build` 和 `npm audit --audit-level=high` 均通过。该项已修复，但后续每次改管理后台 / 官网依赖或 Node 构建链时仍应重新跑 audit 和 build；这不替代线上运行时安全、AccessKey 轮换、真机回归或首封 SLS 告警确认。
- 补充：2026-06-16 按官方组件元数据继续校准版本策略：Android 已升级当前 `compileSdk 36 / AGP 8.13.2` 下可稳定构建的低风险依赖，`core-ktx 1.19.0` 实测要求 `compileSdk 37` 和 AGP 9.1+ 因而暂缓；AGP 9、Kotlin 2.4、Compose BOM 2026.05、OkHttp 5、TypeScript 6 等大版本或渲染 / 构建敏感升级不能在上线前顺手追新，后续必须单独开回归窗口。生产 ECS 当前 Ubuntu 22.04 LTS 仍在标准维护期内，不建议原地大版本升级；24.04 LTS 作为新机迁移目标。剩余风险不是“系统太旧”，而是新版本迁移时要重新验证 Nginx、证书、Cloud Assistant、Logtail、云监控、Go 编译和真机主链。
- 补充：2026-06-16 公网黑盒巡检已把安全响应头纳入自动门禁，避免 Nginx、官网 / 后台部署脚本或证书重做时丢失 HSTS、CSP、Permissions Policy、`nosniff`、`X-Frame-Options` 和 Referrer Policy 仍显示假绿。当前公网实测通过；剩余安全风险仍是 AccessKey 最小权限轮换、首封 SLS 告警邮件确认、真机回归，以及真实攻击放量后是否需要 WAF / 高防。
- 补充：2026-06-16 当前单 ECS 直连公网阶段的真实 IP 链路已收口：Nginx 覆盖 `X-Real-IP` 和 `X-Forwarded-For` 为 `$remote_addr`，Go 只信任来自本机 / 内网代理的转发头；`check-ecs-readiness.ps1` 已把 API / 后台代理头纳入门禁，避免 `$proxy_add_x_forwarded_for` 回潮导致客户端伪造 XFF 链进入应用。剩余风险是未来接 SLB / WAF / CDN 时必须重新配置可信上游真实 IP 链路，否则 `$remote_addr` 可能变成负载均衡内网 IP，影响限流、地区推断和日志归因。
- 补充：买服务器前高并发巡检结论已记录到 [pre-server-feature-audit.md](D:/wuhao/docs/runbooks/pre-server-feature-audit.md)。Go 语言本身不是当前瓶颈；首版单实例可跑早期。OSS 资源包、Bucket 生命周期、代码存储后端和生产 ECS OSS 配置已经就绪；若要多后端实例，仍需先用真实 App 链路验证 `/upload`、`/uploads/` 与模型公网拉图。数据库迁移已补 MySQL 全局锁，后续若进入正式多实例滚动发布，再考虑升级为单独迁移步骤和 `schema_migrations` 表
- 后续动作：下一步优先跟进 App 公安备案、手机号登录、主聊天和图片问诊真机回归；上线前轮换已暴露过的主账号 AccessKey，优先改成专用 RAM 用户 / 最小权限凭证。网站 ICP 备案号、网站公安备案号和 App 备案号已分别补到官网 footer / App 设置页底部与协议基础信息；OSS 上传后端环境变量和最小权限 RAM 凭证已写入 ECS，`/healthz upload_storage=oss` 已验证；继续验证真实 `/upload`、`/uploads/`、模型拉图和主聊天流。备份当前有 7 天默认策略，正式数据进入后再确认是否延长保留时间或加密 / 跨地域备份；SLS 当前已接 Go 后端请求日志、Nginx error log 和 5 条 AlertHub 最小告警，云监控已接 ECS / RDS / Redis 资源水位邮件告警，后续要补的是首封 SLS 告警邮件送达确认、普通短信发送用量 / 模型成本告警和后台排障入口，而不是全量采集所有日志。若后续重新启用 SAE，则再回填 [deploy-sae.md](D:/wuhao/docs/runbooks/deploy-sae.md) 的镜像构建、环境变量、健康检查和回滚入口。按 [model-key-pool.md](D:/wuhao/docs/runbooks/model-key-pool.md) 固化模型 Key 所属账号、充值告警和轮换责任；RDS 规格确认后再按真实连接数配置 `MYSQL_MAX_OPEN_CONNS` 等连接池环境变量。当前本机阿里云 CLI 可读 ECS / RDS / OSS 资源包 / SLS 开通状态并可用 Cloud Assistant 运维 ECS；真实 AccessKey、公安备案数据码不进入仓库或文档，后续稳定后应轮换已暴露过的主账号 Key

## R8 结构化长期资产抽取尚未落地

- 状态：未关闭
- 说明：`server-go` 已新增 `session_round_archive` 保存成功完成轮次，并按 30 天滚动保留；`/api/session/snapshot` 的 `a_rounds_for_ui` 可优先返回 30 天内最近 30 轮归档。当前只保留一份自然语言记忆文档，按“短期记忆 / 长期记忆 / 用户画像 / 农业事件”四块覆盖更新；这些原始归档暂只是“可恢复 / 可批处理”的材料，尚未拆成独立结构化字段。
- 风险：如果后续迟迟不做批量抽取，30 天外原始问答会被滚动删除；长期护城河目前先停留在一份自然语言记忆文档，尚未形成更稳定的结构化长期记忆、用户画像和农业事件记录。
- 后续动作：后续评估结构化记忆 schema、更新频率和抽取模型，优先用归档记录做离线 / 低频批处理，不在当前第一刀里实时抽取、不把归档内容每轮喂给模型；图片文件本身的 OSS / 本地 uploads 生命周期还需单独按成本和隐私策略配置。

## R9 图片发送后台兜底仍受系统调度和租约窗口影响

- 状态：未关闭
- 说明：Android 当前已为带图片发送接入 WorkManager 延迟兜底，覆盖“图片已进本地消息但 App 被杀、前台没来得及可靠送到后端”的场景；它不接管正常前台 SSE 直播，也不直接写 UI 聊天窗口。前台活跃标记、唯一 work 名、远端启动保护窗，以及后端 `chat_stream_inflight` 进行中锁 + lease token 共同用于避免同一 `client_msg_id` 重复启动上游模型流。后端 replay 真源已改为 `session_round_ledger` / 轮次归档成功，服务端只在归档成功后才向客户端发送 SSE `[DONE]`，避免“客户端已收完成态，但回答没归档”时返回空 replay
- 补充：额度扣减在轮次归档成功后执行；归档事务会先写 `quota_consume_outbox`，`ConsumeOnDone` 成功后标记 done，临时失败会按同一 `client_msg_id` 短重试，仍失败则留给后台 worker 继续补偿，重复扣由 `quota_ledger` 唯一键防住。replay 现在只恢复已归档答案，不再按当前档位 / 当前日期补扣旧轮次，避免跨日或会员档位变化后误扣。若数据库连续异常，风险主要表现为“待补扣队列堆积”，后台总览 / 监控面板会显示待处理数量，不把风险转成“乱扣用户次数”或“用户完整答案变失败态”。
- 补充：2026-06-17 只读巡检发现一个低概率边界：主聊天完成归档后的短重试扣次路径仍按重试当下时间调用 `ConsumeOnDone`，而持久化后台补偿队列会按归档记录里的完成时间补扣。只有“归档成功、首次扣次临时失败、短重试窗口刚好跨上海自然日或会员档位刚变化”时，短重试可能和后台补偿的归属时间不完全一致；当前不影响礼品卡、正常扣次或 replay 成功恢复，也不建议临近代理测试前硬改。后续小修可把短重试也统一传入完成时间并补单测。
- 补充：主模型自动开流重试已从 2 次收紧为 1 次，Android 前台流的自动 stream retry 已关闭。WorkManager 后台兜底只针对同一条 pending 图片消息，在图片上传失败、网络中断、流异常结束、`409 STREAM_IN_PROGRESS`、限流或临时上游错误时用同一 `client_msg_id` 退避重试；普通可恢复失败最多重试 5 次后移除 pending，避免弱网一抖就丢消息，也避免无限反复开流。`chat_stream_inflight` 获取结果改为校验 lease token，数据库新增同一 `user_id` 活跃流唯一约束，降低不同 `client_msg_id` 并发绕过额度预检查并多开 Qwen3.5-Plus 的风险；旧 `/api/session/round_complete`、`/api/session/b`、`/api/session/c` 已返回 410，不再参与主链
- 补充：买服务器前“主聊天与图片发送”巡检已记录到 [pre-server-feature-audit.md](D:/wuhao/docs/runbooks/pre-server-feature-audit.md)，当前没有发现旧直连模型、旧完成接口、旧 active-zone、旧图片手势或旧上传通道在运行时并存；后续风险主要集中在真实公网 https 图片链、首版单实例 / OSS 取舍、弱网多图、后台恢复和多实例进程内保护迁移
- 补充：买服务器前“记忆文档与模型调用”巡检已记录到 [pre-server-feature-audit.md](D:/wuhao/docs/runbooks/pre-server-feature-audit.md)，当前记忆文档触发频率、`pending_retry_b`、60 秒超时和 `round_total` 写回校验都与现行口径一致；摘要固定 `qwen-plus`，不再按层灰度。2026-06-16 已在本进程运行中保护之外补 Redis TTL 租约，避免多后端实例同时处理同一账号摘要；若 Redis 不可用或租约未拿到，本轮摘要会跳过并保留 pending 等后续重试。
- 风险：WorkManager 不是实时任务，系统可能延后执行；后端进行中锁当前用 30 分钟租约防死锁，若服务进程极端卡死且租约过期，后续请求才会重新接管。这个设计优先保护成本，不承诺像前台直播一样立刻可见；如果主模型上游已经被调用、但轮次还没归档就发生进程崩溃 / 上游断流 / 归档失败，同一 `client_msg_id` 后续仍可能重新开一次 Qwen3.5-Plus；当前已把自动开流重试限制在后台同一 pending 消息的有限次数内。如果模型已经吐完但归档写库本身失败，客户端不会收到完成态，该轮仍可能需要用户重试或人工排障。归档已成功但扣次失败的场景已有 `quota_consume_outbox` 持久补偿队列兜底，剩余风险是连续数据库异常导致待补扣堆积，需要后台“待补扣”指标和后续对账脚本观察。记忆文档摘要当前已有 60 秒单次提取超时、本进程运行中保护、Redis TTL 租约和 `round_total` 写回校验；剩余风险是 Redis 短时不可用时摘要会留 pending 等后续重试，以及多 ECS 灰度时仍需观察是否存在租约超时后重复摘要。
- 后续动作：短期把这版作为保守兜底真机观察，重点看切后台 / 杀进程后的图片消息是否能恢复、UI 是否不会消失；如果后续要做到 App 被杀后也像前台一样实时可见，需要后端提供更完整的进行中状态查询 / 结果缓存，再决定是否升级成长任务或服务通知方案

## R10 手机号登录生产接入仍需收口

- 状态：未关闭
- 说明：当前后端业务接口和 `/upload` 都要求身份头 / token。服务端已新增手机号账号表、登录 session 表、旧本机 `user_id` 迁移表和 v2 bearer token；Android 已新增登录门、账号 token 保存和普通短信验证码登录 UI。2026-06-13 起新包登录页只保留一个可见入口：自有 Compose 手机号 + 验证码表单，发送验证码调用 `/api/auth/sms/send`，登录调用 `/api/auth/sms/login`；不再展示本机号码一键登录，不再拉起阿里云融合认证 SDK，不再申请 `READ_PHONE_STATE / ACCESS_WIFI_STATE / CHANGE_NETWORK_STATE`。登录成功后使用账号ID `acct_...`，底层字段仍可能叫 `user_id`，生产语义统一按账号ID理解；手机号只是注册 / 登录凭证和脱敏展示信息，不作为业务主键。Android 登录 payload 会提交 `legacy_user_id` 作为旧本机身份线索；2026-06-13 起服务端默认只接受可由旧 bearer token 证明的 legacy ID，不再凭裸本机 UUID 合并资产，且不接受 `acct_...` 作为 legacy bridge，避免账号之间互相合并。`user_id_migrations` 既有映射已经收紧为不可改写，同一旧 ID 若已映射到另一个账号，本次登录不会重映射或合并资产；登录日志会脱敏记录旧 ID 迁移状态，冲突跳过不再被误写成 accepted；跨 Plus / Pro 合并时，被 Pro 覆盖的 Plus 剩余价值会折成 `upgrade_credits`，避免会员价值静默丢失。2026-06-11 已清理生产库旧测试本机 ID 残留，2026-06-13 真机联调后生产库已有首个 `acct_...` 账号，当前没有非 `acct_...` 用户资产归属；后端正式 token、严格鉴权 session 和 Android 本地登录态都已加 `acct_...` 保险丝
- 补充：代码仍保留裸 `X-User-Id` 作为开发 / 迁移期兜底，但公开生产必须保持 `AUTH_STRICT=true`，此时裸 header 和旧无 `session_id` bearer token 默认都会被拒绝；Android 已移除 `SESSION_API_TOKEN` 静态注入和运行时登录绕过，正式登录只使用后端按真实手机号账号签发的 per-user session token
- 补充：Redis 认证限流已覆盖普通短信发送和普通短信登录校验；短信发送按手机号 hash + IP hash 和 IP hash 两层限流，短信登录按手机号 hash + IP hash 限流。服务端旧融合认证 token / login 路由默认返回 `410 fusion_auth_disabled`，只有显式 `AUTH_FUSION_COMPAT_ENABLED=true` 的极短兼容窗口才会使用对应限流；新 Android 主 UI 不调用
- 风险：这还不是完整可上线登录：阿里云短信签名 `北京农技千问科技` 和验证码模板 `SMS_507135108` 已审核通过，用户已购买普通短信服务国内通用短信套餐包，后端 `/healthz` 显示 `sms=ok / redis=ok`；但新 Android 包仍需要真机重新安装并完整回归“发送验证码 -> 输入验证码 -> 登录 -> 进入主聊天”。旧验证码不要复用。普通短信是成本入口，前端 60 秒倒计时、同手机号 5/10min、同 IP 20/10min 和登录校验 10/10min 的限流必须保留；后续还要在阿里云短信控制台开启验证码防盗刷监控、发送量预警、套餐包余量预警和发送频率预警。已暴露过的主账号 AccessKey 需要在上线前轮换；旧 `X-User-Id` 兜底在公开生产仍需严格关闭，旧无 `session_id` bearer token 也不应在公开生产兼容。旧本机资产迁移现在默认只接受旧 token 证明，不再接受未证明 UUID；若未来为极短迁移窗口设置 `AUTH_ALLOW_UNPROVEN_LEGACY_UUID=true`，必须有审计复查并且 readiness 会失败，不能作为公开生产常态。所有会员、额度、加油包、订单、礼品卡、帮助反馈、App 日志、记忆文档、聊天归档和注销申请都必须继续收敛到同一个账号ID，后续接支付不能再按手机号或本机 ID 分叉
- 后续动作：正式公开上线前必须完成 AccessKey 轮换、HTTPS 公网入口下的普通短信验证码登录真机回归和旧 `X-User-Id` / 旧 bearer token 兜底隔离；Redis 验证码 / 限流已接入；`scripts/check-android-build-parity.ps1` 和 GitHub Android CI 已负责拦截 debug / release 后端、签名、普通短信主链、融合认证 SDK / AAR、电话状态权限、明文网络配置和 debug-only 预览面板再次分叉；生产环境保持 `APP_SECRET` 与 `AUTH_STRICT=true`，不要设置 `AUTH_ALLOW_LEGACY_TOKEN`、`AUTH_ALLOW_UNPROVEN_LEGACY_UUID` 或 `AUTH_FUSION_COMPAT_ENABLED`。`scripts/check-ecs-readiness.ps1` 现在会硬拦生产误开这些兼容开关，让这条不只停留在文档规则

## R11 历史模型 Key 轮换确认

- 状态：未关闭
- 说明：Android 旧直连模型链和 `BuildConfig.API_KEY` 注入已从代码中清理，主链只允许经由后端 `/api/chat/stream` 调模型
- 风险：如果历史调试 APK / 旧包曾经打入真实模型 Key，仍应按密钥可能泄露处理；这不是当前代码风险，但属于上线前安全检查项
- 后续动作：确认历史包是否曾打入真实 Key；若有，轮换对应模型服务密钥。后续代码变更禁止重新引入 Android 客户端模型 Key 注入或直连模型客户端

## R12 会员订单接口仍需接真实支付回调

- 状态：未关闭
- 说明：Android 会员中心当前只展示支付占位提示，不会调用后端下单 / 续费 / 升级 / 加油包接口；`server-go` 里现有 `/api/tier/renew_plus`、`/api/tier/renew_pro`、`/api/tier/upgrade_plus_to_pro`、`/api/topup/buy` 仍是开发期直接变更接口，但默认已返回 `PAYMENT_NOT_CONFIGURED`，只有显式设置 `ALLOW_DEV_ORDER_ENDPOINTS=true` 且当前环境明确为 `local / dev / development / test` 时才允许本地调试，缺失环境名也按关闭处理
- 风险：这些接口仍不是正式支付真源；如果内测环境误开 `ALLOW_DEV_ORDER_ENDPOINTS=true`，非 App 客户端理论上仍可绕过真实支付直接请求会员变更。这不影响当前 Android UI 展示，也不影响每日额度 / 升级补偿 / 加油包扣次顺序本身，但属于上线前必须继续收口的业务安全风险
- 补充：会员/额度巡检结论已记录到 [pre-server-feature-audit.md](D:/wuhao/docs/runbooks/pre-server-feature-audit.md)。当前 Android 开通 / 升级 / 加油包按钮只提示“支付功能暂不可用”，没有调用订单接口；因此现阶段能继续作为规则展示，但买服务器本身不等于会员可以直接开卖
- 补充：买服务器前“支付真实接入”巡检已记录到 [pre-server-feature-audit.md](D:/wuhao/docs/runbooks/pre-server-feature-audit.md)，并新增 [payments.md](D:/wuhao/docs/runbooks/payments.md)。当前没有真实支付渠道、SDK、自动续费、退款或对账；`orders` 表也只是开发期成功结果记录，不是正式支付订单表。管理后台订单页已能按账号ID或全局最近记录只读查询 `orders`，用于核查开发期会员变更 / 权益来源，但不提供补发、退款、对账或手动改权益
- 补充：2026-06-13 按微信支付 / 支付宝官方文档重新校准 [payments.md](D:/wuhao/docs/runbooks/payments.md)。微信 App 支付需要移动应用 AppID、商户号 `mchid`、商户 API 证书 / 序列号、APIv3 密钥、微信支付公钥或平台证书，回调要验签并解密；支付宝 App 支付需要开放平台应用、APP 支付产品、AppID、RSA2 私钥 / 公钥或证书材料，服务端调用 `alipay.trade.app.pay` 生成 `orderStr`。两家都必须由服务端创建订单、服务端验签回调 / 查单、服务端幂等发权益；Android 只调 SDK 和刷新订单 / 会员状态，不能保存密钥，也不能把同步返回或客户端“支付成功”当作发权益依据。当前等用户申请并提供商户号 / AppID / 证书 / 密钥后再落正式代码，不为了赶进度做假支付或开发期直改会员。
- 补充：2026-06-15 按支付宝官方文档补充 APP 支付上线边界：生产环境正式调支付宝 App 支付前，开放平台应用需要完成创建、配置、审核上线，并开通 APP 支付产品；应用未上线、未开通 APP 支付或 AppID 配置不对时，生产链路可能报“APPID 应用未上线 / 未开通 App 支付”等错误。未上线或审核中的阶段只按沙箱环境联调，不作为真实扣费入口。
- 补充：2026-06-14 会员 / 礼品卡规则巡检已补齐到当前口径：Android 会员展示常量集中到 `MembershipProductRules.kt`，Plus 为 19.9 元 / 月、Pro 为 29.9 元 / 月、加油包为 6 元 / 80 次，免费 / Plus / Pro 每日基础额度分别为 6 / 25 / 40 次。每日基础额度按上海日期重置，不结转、不当作余额；Plus / Pro 到期或缺少有效到期时间后按免费额度计算。礼品卡会员写入同一份后端 `user_entitlement`，所以有效 Plus / Pro 礼品卡用户与付费会员走同一套会员中心展示、加油包购买资格和 Plus 升 Pro 补偿规则。Plus 升 Pro 的剩余价值保护口径为：当天 Plus 未用完的基础次数 + 剩余完整 Plus 有效天数 * Plus 每日额度，写入升级补偿余额，避免把仍有效的 Plus 价值简单抹掉；这只是替换会员层级时的补偿，不改变“日额度不结转”的常规规则。真实支付仍未接入，Android 购买 / 升级 / 加油包按钮继续只展示支付占位，不发放权益。
- 补充：2026-06-15 管理后台正式上架检查已把“支付未接入”的状态从红色阻塞收窄为 attention：只要 Android 购买入口保持关闭、生产开发期订单端点保持关闭，它不阻塞免费版、礼品卡内测或不含内购的正式上架；但这不代表支付完成，真实收费前仍必须按本风险项接入服务端订单、渠道回调验签、对账、退款和幂等权益发放。
- 补充：2026-06-15 新增只读 [check-payment-readiness.ps1](D:/wuhao/scripts/check-payment-readiness.ps1)，并接入上线总门禁。脚本会确认 Android 会员 / 加油包入口仍关闭、Android 未调用开发期订单接口、后端开发期订单接口仍有 `PAYMENT_NOT_CONFIGURED` 防线、公网 `/healthz` 显示 `dev_order_endpoints=false`，并输出微信 / 支付宝回调 URL 建议。后续脚本又补充支付宝沙箱和微信 App 支付前置配置缺口检查：当前本机缺支付宝沙箱 AppID / 应用私钥 / 支付宝公钥，也缺微信 AppID / 商户号 / 商户私钥 / 证书序列号 / APIv3 Key；脚本只输出缺哪些配置，不打印密钥值。它只证明当前“未开放收费”状态安全并列出调试材料缺口，不代表真实支付已完成。
- 补充：2026-06-15 支付当前只做“假测试 / 安全测试”，不做真实扣费测试：Android 主界面会员入口、设置页会员入口和会员中心内灰色未开放套餐 / 加油包按钮点击时都会给“不扣费”反馈并上报自有 App 日志 `payment.unavailable_clicked`，attrs 只带 `source`；`check-payment-readiness.ps1` 会把该日志埋点、入口关闭、生产 `dev_order_endpoints=false`、开发期订单防线和两家材料缺口一起检查。总门禁在正式支付未配置时把这一步显示为 `payment closed guard` attention，表示“未开放收费状态是安全的”，不是“真实支付已完成”。支付宝沙箱真实下单 / 异步通知验签、微信 App 下单 / 拉起支付 / 回调解密验签仍必须等平台 AppID、商户号、公私钥、证书和 APIv3 Key 等材料齐全后再测，不能凭代码自行伪造。
- 补充：2026-06-17 开发期订单回放已按全局 `order_id`、账号ID和商品类型一起校验；同一 `order_id` 属于其他账号，或同一账号把 Plus / Pro / Plus 升 Pro / 加油包不同商品混用时，都会返回 `ORDER_ID_CONFLICT`，不再把旧订单回放到错误权益上。Plus 升 Pro 补偿计算也统一使用同一个请求时刻派生上海日期，避免极端跨午夜时今日剩余次数和剩余整天数不一致。当前权益口径按用户拍板固化：加油包次数和 Plus 升 Pro 生成的升级补偿次数永久有效，不随会员到期清零；Plus 升 Pro 不做现金折扣抵扣，按 Pro 开通价升级并把 Plus 剩余每日权益折成永久补偿次数。该改动只降低本地 / 测试环境假订单误判风险，不开放生产支付；生产仍要求 `dev_order_endpoints=false`，真实收费前仍必须接服务端订单、渠道回调、验签、查单、对账、退款和幂等发权益。
- 补充：2026-06-16 `check-payment-readiness.ps1` 继续锁后台订单只读边界：会检查订单页文案明确只做核查、不提供补发 / 退款 / 手动改权益、不提供支付成功模拟或手动发放入口；同时要求服务端只注册 `GET /admin-api/v1/orders`，不允许出现 `POST / PUT / PATCH / DELETE /admin-api/v1/orders` 写路由。该门禁只证明未开放支付阶段不会在后台误点发权益，不代表真实支付、退款或对账已接入。
- 后续动作：接入真实支付时，把会员变更收敛到服务端验签后的支付回调 / 对账流程，并移除或彻底隔离开发期直接变更接口；生产环境保持 `ALLOW_DEV_ORDER_ENDPOINTS` 未设置 / false，并配置 `APP_ENV=production` 或等价环境变量作为额外保险。当前手机号账号、per-user token、`AUTH_STRICT=true` 和 `acct_...` 资产归属护栏已落地，后续支付接入必须沿用同一个账号ID，不允许再按手机号、本机 ID 或客户端支付结果分叉发权益。正式订单表需覆盖渠道订单号、金额、商品、状态、回调、退款和幂等发放结果；申请支付前资料、微信 / 支付宝官方接入口径和回调地址建议以 [payments.md](D:/wuhao/docs/runbooks/payments.md) 为准

## R13 今日农情生成质量和失败告警仍需上线观察

- 状态：未关闭
- 说明：今日农情首版已接入独立后端链路和 Android UI-only 内容；它不影响聊天主链、不扣用户问诊次数、不进入用户记忆文档或归档。Android 主界面仍通过 `ChatTimelineItem.TodayAgriCard` 插入同一个正向聊天 timeline，但 2026-06-16 已把视觉从独立边框卡片收成普通 AI 文本块，方便像普通回答一样阅读和滑动；2026-06-17 主聊天展示又收为“当天第一次进入主界面展示一次”，当天已插入可见主聊天 timeline 后，本地记录已展示日期，同日重开不再反复插入，设置页“今日农情”历史入口仍可查看最近记录。后端接口、数据库表和 runbook 名称仍可能沿用 `card`，这是历史命名，不代表它是聊天历史消息。当前生成固定 `qwen3.5-plus + OpenAI compatible chat/completions + enable_search=true + search_strategy=turbo + forced_search=true + enable_source=true + enable_thinking=false`，不保留其它模型候选或环境变量切换入口。生成前会把过去 7 天已 ready 的今日农情喂给模型要求去重；提示词版本 `2026-06-15-v77` 已压缩为通用化短任务说明，当前唯一硬数量要求是 3 条，发布、Android 展示和后台预览都要求 3 条标题 / 摘要完整 item，少于 3 条视为不完整；若历史缓存或手工写入超过 3 条，公开接口只下发前 3 条，避免用户侧出现 4 条。v77 口径是不限制具体作物，选题以种植方面新闻为主，更优先选择对生产有实际参考价值或技术含量的内容；天气、气象、防灾或抢收可以作为其中一个角度，但不要三条都写成天气预报，且需说明对农事安排、防灾减损或田间管理的影响。种植侧、养殖水产不要、广告软文 / 假新闻 / 旧闻 / 重复事件 / 标题党等质量方向主要靠提示词、探针、后台运营复核与日志观察控制。服务端当前只保留 JSON 结构、3 条标题 / 摘要非空 item、公开数量一致和私网 / 本机 URL 安全兜底，不按可信域名、主题词、发布日期、历史链接、重复标题、养殖词、广告词或摘要字数做发布阻断。生成最多 2 次，第二次只在首轮解析失败后换检索提示补救，仍走 `turbo`，不切 `agent`；兼容 Chat 链路通常没有结构化搜索来源列表，来源名主要来自模型 JSON 的 `source_name`，来源 URL 只作为内部追溯和后台排查，不下发给 Android 用户文本块。ECS systemd timer 和后台补跑都已落地。
- 风险：如果 `turbo` 返回的材料质量差、主题偏养殖水产、广告软文多、技术含量不足或信息过期，模型仍可能产出不理想；后端当前不靠内容拦截解决这些问题，只有模型输出无法解析成 JSON 或少于 3 条标题 / 摘要可展示 item 时才不发布，前端静默不展示。`qwen3.5-plus` v77 链路仍需持续用生产探针确认 `ok_count`、`model_search_count`、`model_reasoning_tokens`、标题长度、来源名、过期材料、天气占比、实用技术含量、综合指数 / 综合行情凑数和养殖水产 / 广告软文是否被提示词压住；兼容 Chat 链路的 `source_count=0` 不等同于没联网。2026-06-15 v77 生产探针 `runs=3` 为 `ok_count=3/3`，每轮 3 条完整 item，未返回 reasoning tokens，样本包含雨后田管 / 复产、夏收、农垦标准、茶园修复、晚播小麦收官等，摘要约 75-101 字；前两轮仍有天气切入，第三轮技术 / 生产内容更明显，后续仍需观察题材分散度、天气占比、技术含量、来源名和摘要厚度。联网搜索会增加输入 token，并且搜索策略本身也有调用费用，所以仍要坚持“云端每天定时一次 + 后台补跑兜底”，不能改成用户打开 App 时临时生成。当前仅有 `scope=CN` 全国内容，尚未做地区 / 作物个性化；`nongji-daily-agri-failed` AlertHub 告警已落地，但首封告警邮件送达、自动重试策略和运营抽查节奏还需要继续收口。Android 文本块样式仍需真机看首次展示时欢迎语兜底、今日农情自然跟随、发消息后自然上顶、回到底部、长摘要换行、同日重开不重复和滚动体感。
- 补充：买服务器前今日农情巡检已记录到 [pre-server-feature-audit.md](D:/wuhao/docs/runbooks/pre-server-feature-audit.md)。当前没有发现用户打开 App 临时生成、写用户记忆文档、写归档、扣次或伪装成 `ChatMessage` 的旧链路；残余风险主要是质量运营，比如同一事件换标题、养殖水产漏入、广告软文和假新闻需要靠提示词、探针、后台人工抽查与后续告警约束。当前检索阶段已改为全网宽搜、不限定固定网站，发布阶段也不再用可信域名 / 近 7 天链接 / 主题词 / 内容词表做内容拦截；如果模型没有输出可解析 JSON 或少于 3 条标题 / 摘要完整内容，用户侧会静默不展示，需要通过后台补跑或内部探针排查。
- 补充：2026-06-09 已补今日农情坏缓存兜底：用户侧只展示 JSON 合法且结构完整的 ready 卡片，坏 `content_json` 不再把 `/api/today-agri-card` 打成 500；后台今日农情列表会把坏 `content_json / sources_json` 标成行内错误；补跑遇到 ready 但正文不可用的卡片时允许重新生成覆盖。该护栏只解决坏数据可恢复和后台可见性，不代表内容质量、自动告警或个性化已经闭环
- 补充：2026-06-17 Android 今日农情展示已收成普通 AI 文本，仍复用同一个 timeline item，不写入 `ChatMessage`、不进入远端聊天历史、不进记忆、不进归档、不扣次，也不恢复 overlay、sticky、反向列表或关闭动画。为了支持用户紧邻追问“上面第二条 / 刚才农情”，Android 只在当天视觉项后方连续三轮发送里携带 `today_agri_context_day`，后端校验为服务器上海当天后临时注入当天农情作为系统背景，第四轮起自动不带；同日重开后因主聊天不再重复展示今日农情，也不会给后续新发送继续携带今日农情上下文。展示时机按“后端清晨生成，用户当天第一次进主聊天时展示一次”：有历史在历史后，没有历史先显示欢迎语兜底，今日农情只是后方普通文本项；如果用户进入后先开始发送 / 生成，且今日农情还没展示出来，本次运行不再自动插入，避免打断问诊；未写已展示日期时，下次打开仍可按当天未展示处理。内容底边到达或超过 96dp 工作线后，进入同一套底部锚定，后续已有真实消息或进入正常聊天流时，今日农情作为视觉尾部参与回底和静态贴底。后续如果用户要求“远端拉取像正常聊天一样回来”，应先区分“UI 展示像普通文本”和“业务历史真相”；当前拍板仍是独立今日农情接口负责拉取，不把它伪装成用户聊天历史。
- 后续动作：持续观察 SLS 日志关键词 `daily agri generation started` / `daily agri model response received` / `daily agri model output not displayable` / `daily agri card generated` / `generate today agri card failed`，并抽查 `daily_agri_cards` 当天 `status/content_json/error/model/search_strategy/prompt_version`。若连续失败，优先评估调整主提示词、增加探针观察、后台人工复核 / 补跑、自动重试和告警邮件送达确认；不要回到可信域名白名单、固定站点限制、旧 `prompt_intervene` 路线、后端内容过滤或用户打开 App 临时多次调模型

## R14 帮助与反馈首版仍需客服工单能力补齐

- 状态：未关闭
- 说明：帮助与反馈当前已具备后端消息表、轻量会话状态表、用户侧历史 / 发送 / 已读接口，内部读取 / 回复接口，以及网页后台会话列表、详情、状态筛选、搜索、回复、关闭和重开入口；Android 也能展示历史、发送文字 / 图片反馈，并在设置页“帮助与反馈”行用红点提示未读后台 / 系统消息。
- 风险：当前仍不是完整客服系统，缺正式坐席分配、标签、站外推送、客服绩效、SLA、自动归档和消息保存 / 删除规则。公开生产前仍必须保持账号 token 和 `AUTH_STRICT=true`，否则裸 `X-User-Id` 能读写某个用户的帮助与反馈；2026-06-13 起帮助与反馈图片已从问诊图 `uploads/` 3 天游走期分流到 OSS `support/` 30 天生命周期，但客服聊天记录正文、发送人、时间和已读状态仍保存在 MySQL，不会随图片自动过期。用户消息和自动回复已收进同一条 MySQL 命名锁 + 事务路径，避免同一用户并发连发时重复插入自动回复；剩余风险主要是客服运营规则和合规留存规则未完成。
- 补充：2026-06-13 Android 帮助与反馈消息区已改成带稳定 key 的 `LazyColumn` 渲染，长期站内客服对话只渲染可见区域，降低长记录一次性铺开造成的卡顿风险；这不改变 MySQL 聊天记录真源，也不会把客服记录改成边滚边联网加载。
- 后续动作：在统一管理后台继续补坐席分配、标签、站外通知、客服绩效和消息保存 / 删除规则；不要在 Android 端塞后台逻辑。账号注销 / 数据删除规则还要明确帮助与反馈消息和图片是否删除、保留多久、由谁操作。

## R15 自有 APK 更新链路需要上线实机验证

- 状态：未关闭
- 说明：Android “检查更新”已按自有服务器 APK 分发接入，后端通过后台 `app_release_configs` 或 `APP_ANDROID_*` 环境变量返回最新版本；只有更高版本号、HTTPS APK、SHA-256 和文件大小都齐全时才会下发可用更新，客户端下载后会先校验最终 https、文件大小、SHA-256、包名和 `versionCode`，再调起系统安装页；Android 会用 `app_update.*` 自动日志上报检查、下载、校验、安装权限和安装页拉起阶段，后台监控面板已有“检查更新排障”卡。后台上架检查当前不会把“物料齐全”标为正式 ready，只提示可进入真机覆盖安装验证。
- 补充：2026-06-12 客户端也收紧为只有物料完整且版本更高才展示 / 下载更新；下载器遇到缺 SHA、缺文件大小或缺期望 `versionCode` 会 fail closed 并上报失败原因，不再跳过校验。
- 补充：2026-06-13 Android 8+ 跳转“安装未知应用”设置后，用户返回 App 且权限已允许时会自动继续下载 / 安装流程，不再要求回到检查更新弹窗再点一次“立即更新”；不同 ROM 的授权页和系统安装页仍需旧包真机覆盖安装验证。
- 风险：普通 Android App 不能静默安装 APK；Android 8+ 需要用户给本 App “安装未知应用”授权，不同国产 ROM 的授权页和安装页可能存在差异。APK 下载地址、签名证书、`versionCode` 递增、https 证书、包名一致性、哈希配置和安装权限都需要真实环境验证。若后续发布 APK 签名证书变化，系统会拒绝覆盖安装；如果用户已经安装坏包，只能再发更高 `versionCode` 修复包，不能低版本回滚覆盖。后台排障日志能定位阶段，但不能替代旧包真机覆盖安装回归。
- 后续动作：正式发第一版 APK 更新前，按 `docs/runbooks/app-update.md` 用旧包真机验证“检查更新 -> 发现新版本 -> 授权安装未知应用 -> 下载 -> 校验 -> 系统安装页 -> 覆盖安装成功”；发布流程里固定签名证书、`versionCode` 递增规则、文件大小和 SHA-256 记录
- 补充：2026-06-15 已新增 [check-android-release-artifact.ps1](D:/wuhao/scripts/check-android-release-artifact.ps1)，用于直接校验最终 `app-release.apk` 本体的包名、版本、不可调试、权限白名单、固定 release 证书指纹，并输出 `apk_size_bytes` / `apk_sha256` 供后台检查更新填写；`build_apk.bat` 和 `check-launch-readiness.ps1 -IncludeBuilds / -ReleaseGate` 已接入该校验。同日新增 [check-app-update-release-match.ps1](D:/wuhao/scripts/check-app-update-release-match.ps1)，可在正式包上传后台并准备启用后，只读核对后台“检查更新”配置里的 `versionCode / versionName / SHA-256 / 文件大小` 与本地最终 APK 一致，并可选下载后台 APK 链接重算大小和 SHA-256；`check-launch-readiness.ps1 -CheckAppUpdateReleaseMatch` 可把这一步接入发版慢检。但这仍不能证明用户在真机上完成未知来源授权、系统安装页确认和覆盖安装成功；这些仍需旧包真机回归闭环。
- 补充：2026-06-15 继续按 Android 官方包安装口径收口客户端安装页拉起：`AppUpdateInstaller` 现在优先使用系统包安装器 action，失败再回退通用 APK `ACTION_VIEW`；安装包仍只来自 `cacheDir/app_updates` 并通过 `${applicationId}.fileprovider` 授予临时读取权限。`scripts/check-android-build-parity.ps1` 已把 `REQUEST_INSTALL_PACKAGES`、FileProvider `app_updates` 路径、未知来源授权返回后继续同一更新、防重复检查 / 下载、双 intent 安装兜底、SHA / 文件大小 / 包名 / versionCode fail closed 纳入门禁。该补强能减少后续改版删漏安装链路，但不同 ROM 的未知来源授权页和系统安装页仍必须用旧包真机覆盖安装验证。
- 补充：2026-06-15 自更新发版门禁继续收紧：后端、后台保存入口、Android 下载器和最终 APK 校验统一 200MB 上限，后台不能保存客户端永远不会下载 / 安装的超大 APK；`check-app-update-release-match.ps1 -VerifyDownload` 会确认最终下载地址仍是 HTTPS；`check-launch-readiness.ps1 -AppUpdateReleaseGate -AppUpdatePreviousVersionCode <旧包版本号>` 会强制构建、后台 owner smoke、后台物料对账、下载回验和公网 `/api/app/update` 旧包探针，证明本地 APK 与后台配置都大于旧包且旧包会看到 `has_update=true`。这仍不替代真实用户在系统安装页里点确认的覆盖安装回归。
- 补充：2026-06-16 Android 更新体验继续按成熟普通更新收口：无更新时提示“当前没有可用更新”，发现更新时弹“发现新版本”卡片；客户端不再在权限、下载或系统安装页真正拉起前提前记录“已提示版本”，系统安装页返回后会检查当前已安装 `versionCode`，安装未完成会清掉本次提示记录并提示用户稍后继续安装，避免用户取消安装后长期收不到同一版本更新提醒。下载校验在 HTTPS、文件大小、SHA-256、包名和 `versionCode` 外，又补了安装包签名证书 SHA-256 与当前 App 签名一致性检查；这仍不能替代旧包真机覆盖安装验收，不发布新版本仍必须等用户明确口令。
- 补充：2026-06-17 自更新正式物料继续收紧：后端、Android、后台、官网和 `check-app-update-release-match.ps1` 都会拒绝带 `Expires / Signature / OSSAccessKeyId / security-token / x-oss-expires / x-oss-signature / x-oss-credential / x-oss-security-token` 等短期签名参数的 APK URL，也会拒绝外部 APK 域名和非 `/android/releases/` 路径；正式检查更新只接受 `https://download.nongjiqiancha.cn/android/releases/...apk` 这种自有下载域名下的稳定 release 地址。内部测试包默认走 `download.nongjiqiancha.cn` 私有 OSS 短签名下载，但这条链只给用户临时测试，不进入后台正式发布配置。后端按需签名是未来可选方案，未另行实现和验证前不能当作现有正式能力。旧包真机覆盖安装、未知来源授权、系统安装页确认和取消后再次提醒仍是正式发版前必须验收项。

## R16 礼品卡批量发放和精细风控仍未完成

- 状态：未关闭
- 说明：后端礼品卡批次、卡、兑换尝试表、后台创建 / 查询 / 作废接口、用户侧 `POST /api/gift-cards/redeem` 和 Android 设置页真实兑换入口已接入；兑换会事务内发会员权益，并记录成功 / 失败尝试、地区、脱敏 IP 和审计，失败尝试写入和事务提交错误不再静默吞掉。后台已补全局汇总、完整卡码加密保存后页面查看复制、卡码尾号 / 批次 / 账号ID追溯、兑换尝试成功状态 / 失败原因筛选和最近 7 天失败原因聚合。Android 只在后端返回成功后展示成功或“权益已生效”结果并刷新会员权益，没有后端成功结果不能弹成功态。
- 风险：后台当前仍没有批量发放名单、发放对象管理和更细风控统计；完整卡码可在后台页面内查看和复制，但不能写进备注、作废原因、审计 detail、日志、文档或导出文件。本次能力上线前生成的旧卡没有加密字段，后台不能反推出完整卡码。
- 补充：2026-06-16 礼品卡兑换补了同一账号重复提交的幂等成功路径：已由本人成功兑换过的卡再次提交时返回原兑换结果并追加一次成功尝试，响应带 `replay=true`，Android 显示“权益已生效 / 该礼品卡已兑换成功，会员权益已生效，无需重复兑换”，不会重新发权益；其他账号重复提交仍失败。正常兑换也检查 `RowsAffected()==1`，避免并发抢卡时误发权益。后台失败原因筛选已补 `invalid_code`，方便代理测试空卡码 / 格式错误时追溯。剩余风险仍是批量发放名单、发放对象管理、更细风控统计和正式发卡前真实 Plus / Pro 样卡验收；后台“礼品卡成功”这类统计后续要注意它统计的是成功尝试，不等同于唯一发卡数。
- 补充：2026-06-16 用户拍板礼品卡首版不做预约生效 / 未来生效，后台生成卡码后即可兑换，兑换成功后权益立即生效；后端单测已锁住批次创建 `valid_from` 使用当前创建时间，且旧数据或脚本若把 `valid_from` 写到未来，后端兑换、后台礼品卡汇总和监控面板也不再把它作为预约生效门槛，active 且未过期的卡兑换即发权益。Android 礼品卡失败原因现在也会停留在兑换页内，降低用户错过短提示的风险。该补充只收口当前发卡和兑换体验，不代表批量发放名单、发放对象管理、商务发放渠道或更细风控统计已完成。
- 补充：2026-06-16 礼品卡单测继续补样卡边界：Plus 用户兑换 Pro 会生成升级补偿次数并升级到 Pro，Pro 用户兑换 Plus 会被拒绝且不消耗礼品卡。这样能覆盖代理测试时最容易误会的“Plus 可升级 / 低档卡不能覆盖高档权益”规则；真实 Plus / Pro 样卡仍需正式发卡前在 Android 和后台各跑一张验收。
- 补充：2026-06-16 后台礼品卡生成 500 已定位到 `gift_cards` 插入 SQL 占位符数量不一致并修复，新增单测锁住批次生成会真实插入卡行；后台生成失败会返回通用 `internal_error` 并写审计，不把 SQL 细节暴露给页面。2026-06-17 后台生成确认进一步收紧为“张数 + 档位 + 天数”，例如 `3 Pro 30`，服务端和 `check-admin-surface.mjs` 同步锁住，降低管理层试用时误点真实卡的风险。剩余风险仍是正式发卡前用真实 Plus / Pro 样卡验收、批量发放名单、发放对象管理和更细风控统计。
- 后续动作：按 [gift-card.md](D:/wuhao/docs/runbooks/gift-card.md) 补批量发放名单、发放对象管理、商务发放渠道、外部备注和更细风控统计。Android 仍必须坚持只提交用户输入并展示后端结果，没有后端成功结果时不能弹“兑换成功”。

## R17 协议、隐私和风险提示仍需上线前合规复核

- 状态：未关闭
- 说明：Android 设置页已接入本地内置“服务协议 / 隐私政策 / 第三方信息共享清单 / 个人信息收集清单 / 应用权限 / 风险提示”6 个页面，并把入口统一整理到“服务协议”目录页；2026-06-14 已按正式上线口径重写服务协议正文，6 段覆盖服务内容、农业建议边界、用户内容与账号权益、会员 / 支付 / 加油包 / 礼品卡、农资信息与交易边界、禁止行为和协议更新。App 用户可见正文不暴露具体云厂商、模型品牌、后台实现名、供应商账号或旧 SDK 机制；支付文案按页面支持的微信支付、支付宝等正式交易口径说明，但真实支付回调、验签、对账和权益发放仍归 R12 跟进。当前已移除独立“数据管理”入口，“删除所有历史对话”收进“账号管理”页并已接 `POST /api/session/clear` 清理聊天历史、A 层滑窗、记忆文档和 30 天归档
- 风险：这些文案不是律师审定稿；公司主体、ICP备案、支付规则、礼品卡规则、农资交易规则、账号注销、完整个人信息查询 / 删除 / 导出接口、第三方服务 / SDK 清单、图片 OSS 保存周期和真实权限一旦变化，当前文案可能需要同步调整。当前“删除所有历史对话”只覆盖问诊聊天历史和摘要记忆，不等于完整账号注销或全部个人信息删除。2026-06-13 已把首次隐私同意合并到登录页协议勾选：用户同意前只展示登录 UI，不初始化本机业务身份、不补报待发送崩溃日志、不请求后端；但登录页同意文案、协议正文、个人信息查询 / 删除 / 导出和应用市场材料仍需上线前合规复核，不能因为代码已补同意门槛就把本项风险关闭
- 补充：协议和隐私巡检入口已沉淀到 [legal-privacy.md](D:/wuhao/docs/runbooks/legal-privacy.md)，后续新增权限、SDK、支付、短信、手机号登录、OSS / SLS 或隐私政策 URL 时必须同步复核；debug-only 预览面板的协议 / 检查更新卡片也要复用正式组件和正式口径，避免预览面板再引入旧文案或嵌套滚动闪退
- 补充：2026-06-15 已把首版数据留存和成本口径落成只读巡检：问诊图片 3 天、帮助反馈图片 30 天、主聊天完整归档 30 天滚动、SLS 7 天、App 自动日志 30 天低成本排障窗口，并用 [check-data-retention-cost.ps1](D:/wuhao/scripts/check-data-retention-cost.ps1) 监控重点文字表年龄和体量；当前生产重点表合计约 `0.828MB`，成本风险很低。该补丁只证明首版工程口径可见，不等于法务已审定，也不等于客服文字、后台审计、账号注销联动和去标识化任务已经完整闭环。
- 补充：2026-06-16 App 内和官网协议 / 隐私已继续收口正式上线文案：未开放或不可用的微信 / 支付宝入口不会发起真实扣费；礼品卡是权益发放凭证，不等同于支付订单或退款依据；注销账号是申请核验流程，预计 15 个工作日内处理，法定留存、交易、风控、审计记录按要求保留或去标识化。隐私保存期限已把 App 自动日志约 30 天排障窗口和服务端运行日志 7 天分开说明，避免用户误以为所有排障记录只有 7 天或全部长期保存。该文案仍不是律师审定稿，正式公开推广前仍需按最终支付、SDK、权限和注销流程复核。
- 补充：2026-06-16 账号管理页用户可见入口已从“注销账号”改成“申请注销账号”，确认标题和按钮也改为提交申请口径；“清理临时缓存”继续保持简洁正式口径，不扩写成容易吓人的资产删除说明，实际仍只清更新、拍照和临时缓存，不删除聊天、会员、礼品卡或反馈记录。官网隐私政策也补充“页面标明暂未开放或不可购买的入口不会发起真实扣费”。这些文案降低误读风险，但账号注销真实处理、个人信息删除 / 去标识化范围和法务审定仍未关闭。
- 后续动作：正式上架 / 公开推广前，按真实公司主体、备案、权限、SDK、第三方服务清单、数据保存周期、支付和账号注销流程复核三份文案；若新增定位、通知、相机、相册或存储权限，必须同步修改隐私政策、Manifest 和项目记忆

## R18 备案、软著和应用商店物料尚未最终稳定

- 状态：未关闭
- 说明：当前 App 名称按“农技千查”推进，公司主体仍是“北京农技千问科技有限公司”；Android 对外包名已定为 `com.nongjiqiancha`，本机已生成 release 签名并导出备案用 MD5 / SHA1 / SHA256 / RSA 公钥信息。当前已按 `农技千查 Android应用软件 V1.0` 生成软著申请信息草稿、操作说明书和源代码前后各 30 页摘录材料，材料位于 `D:/Desktop/农技千查软著材料`，但尚未由公司 / 授权经办人在中国版权保护中心提交并获证。最终图标仍需重做以避开应用商店“图标重复 / 混淆”类打回，软著 / 电子版权证书、备案号、正式截图、测试账号和应用商店物料还未全部落地
- 风险：备案和应用商店审核虽然不是同一个流程，但 App 名称、图标、包名、签名、公钥、协议、隐私政策和展示物料如果反复变化，可能导致备案变更、应用市场反复打回或上架材料互相不一致。旧内测包 `com.nongjiqianwen` 不能通过检查更新覆盖安装成新包 `com.nongjiqiancha`；如果存在旧包测试机，需要卸载旧包重装，不能把这次包名切换当普通自更新
- 后续动作：按 [go-live-plan.md](D:/wuhao/docs/runbooks/go-live-plan.md) 固定最终图标、包名、签名证书和软著 / 电子版权材料；网站 ICP 已通过，网站公安备案号已下发并补到官网 footer，根域名官网和 `api.nongjiqiancha.cn` HTTPS 已完成，App 备案已通过并补 `京ICP备2026031728号-2A` 展示，继续跟进 App 公安备案、软著 / 电子版权和应用商店物料
- 补充：2026-06-16 官网移动端发现部分手机浏览器会把首页 H1 中文按一字一列竖排，原因是旧 CSS 为防长单词撑破布局使用了过激的 `overflow-wrap:anywhere` 并在某些触屏浏览器上没有及时切到移动单列。当前已在 `site/src/styles.css` 改为中文标题 `word-break: keep-all`、普通正文只对长英文 / URL 做 `overflow-wrap: break-word`，触屏设备直接走单列移动布局；本地 390px 预览和公网 `nongjiqiancha.cn / www.nongjiqiancha.cn` CSS 均已确认不再横向溢出或一字一列。若用户手机仍看到竖排，优先按浏览器缓存处理，刷新或清缓存后复核。
