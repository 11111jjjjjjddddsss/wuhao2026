# 当前状态

最后更新：2026-06-07

## 项目概况

- 项目：农技千查
- 目标：面向农作物问题提供农业技术参考建议，不提供绝对诊断
- 当前仓库两条主线：Android 客户端 `app`，Go 后端 `server-go`

## 当前代码真相

- Android 端当前使用 Jetpack Compose 聊天界面，不再依赖 WebView 模板页面
- Android 对外安装身份已定为 `applicationId = com.nongjiqiancha`，App 名称为“农技千查”。Kotlin 源码包 / Gradle namespace 暂保留 `com.nongjiqianwen` 作为内部代码命名空间，不决定备案或安装身份；FileProvider authority 会随 `${applicationId}` 变为 `com.nongjiqiancha.fileprovider`。本机已生成固定 release 签名，备案用 MD5 / SHA1 / SHA256 / RSA 公钥信息保存在 `%USERPROFILE%\\.nongjiqiancha\\android-release-public-info.txt`，签名配置保存在 `%USERPROFILE%\\.nongjiqiancha\\android-release-signing.properties`，私钥和密码不进仓库。Android 构建默认使用 `UPLOAD_BASE_URL=https://api.nongjiqiancha.cn`，Android Studio 直接 Run 的 debug / 测试包也应接正式 HTTPS 后端；仍可用 `-PUPLOAD_BASE_URL=...` 或环境变量覆盖。正式 release 构建必须配置固定签名，且有效 `UPLOAD_BASE_URL` 必须是 https；Android 已移除静态 `SESSION_API_TOKEN` 运行时绕过，正式包只走手机号账号 session token。历史内测旧包 `com.nongjiqianwen` 不能通过检查更新覆盖安装成新包，测试机需要卸旧包重装；后续自更新只支持 `com.nongjiqiancha` 同包名升级
- Android 客户端已清理旧直连模型链：不再包含 `QwenClient.kt` / `ModelService.kt` / `ModelParams.kt`，也不再从 `local.properties`、`BAILIAN_API_KEY` 或 Gradle property 注入模型 `API_KEY`。主对话和图片问诊只能通过后端 `/api/chat/stream` 发起模型调用
- Android Auto Backup / Data Extraction 当前已关闭并显式排除：`allowBackup=false`，同时通过 `backup_rules.xml` / `data_extraction_rules.xml` 排除 cloud backup、device transfer、credential-protected / device-protected shared preferences、files、databases 和 external 数据。本地聊天窗口快照、流式草稿、`app_ids`、旧 UI metrics 等都只作为本机运行时缓存，不允许被系统云备份 / 设备迁移在清数据 / 重装后恢复成旧 UI 状态。后端仍是业务真相来源
- Go 后端当前同时保存两类资产：`session_ab.a_json` 仍是 A 层滑窗，写入新轮次后会裁剪到 Free / Plus 6 轮、Pro 9 轮；`b_summary` 当前承载 B 层通用短期记忆，定位是全场景当前主线 / 当前事务短期承接，用来保留最近对话主线，让下一轮能自然接得住，不承载长期画像；`c_summary` 当前承载 C 层长期通用记忆，仍是单个文本字段，但提示词要求分成“长期通用记忆 / 用户画像 / 农业相关重点事件记忆”三块，既保留长期通用背景和用户画像，也低频承接重点农业事件，不是通用知识库或病例流水账；成功完成的问答轮次会额外写入 `session_round_archive`，按 30 天滚动保留，用于 UI 历史恢复、C 层最近 20 轮完整问答提取和后续批量抽取。`/api/chat/stream` 允许纯文字、纯图片、图文混合；纯图片且用户未输入文字时，后端会给模型补一条内部说明，引导先基于图片可见信息做农业技术参考判断并追问必要信息，该说明不作为用户可见消息。当前不单独落复杂农事事件表 / 状态卡，后续如三块文本不够用再评估结构化字段
- `/api/session/snapshot` 当前继续返回 `a_json` / `a_rounds_full` 作为 A 层窗口，同时 `a_rounds_for_ui` 会优先返回 30 天内最近 30 轮 `session_round_archive`。前端本地仍会用 `LOCAL_RENDER_ROUND_LIMIT = 30` 裁 UI 窗口；换机 / 重装后只要用户身份能对上后端 `user_id`，UI 可拉到最近 30 轮业务聊天记录。当前手机号账号骨架已落地：后端新增手机号账号、登录 session 和旧 `user_id` 迁移表，Android 已加登录门和本地 auth token 保存；登录成功后使用账号 `user_id`；旧本机 `user_id` 只作为受控迁移桥，服务端只有在请求同时提供可验证该旧身份的 bearer token 时才允许迁移，Android 手机号登录 payload 当前不再默认提交 `legacy_user_id`。阿里云融合认证 Android 方案已通过 CLI 创建并把 DYPNS AccessKey / Secret、`DYPNS_FUSION_SCHEME_CODE`、包名和签名写入本机密钥文件与 ECS 环境；短信签名和验证码模板已通过 CLI 配置并写入 ECS，`/healthz` 已显示 `dypns=ok / dypns_fusion=ok / dypns_sms=ok`；Android 一键登录 SDK / AAR 已导入并接到登录页，真机登录联调还没完全跑完，且已暴露过的主账号 AccessKey 上线前必须轮换；Redis-backed 手机号认证限流已部署到 ECS 并健康；未登录 / 未配置完成时，清数据后仍无法自动识别旧本机用户
- Android 手机号登录页当前只保留品牌标题、本机号码一键登录、验证码登录和协议勾选区；已删除登录后同步 / 旧记录迁移两句说明文案。登录页《服务协议》《隐私政策》会弹出设置页同一套本地内置正文，不再额外维护第二份登录页协议内容；一键登录按钮现在会在用户同意协议后按需申请电话状态权限、拉取后端 fusion token 并启动阿里云融合认证 SDK；源码 manifest 已显式声明一键登录所需的 `READ_PHONE_STATE` 和设备网络 / Wi-Fi 状态权限，release 前仍需检查 merged manifest；SDK 半程校验走后端 `/api/auth/fusion/verify` 只校验 verify token、不签发账号 session，Android 同时要求响应体 `ok=true` 才算半程通过，最终成功节点才把 SDK 返回的 verify token 交给后端 `/api/auth/fusion/login` 换取账号 token，失败 / 取消后切回验证码登录
- 短期限流当前只做防刷，不做业务真相：Redis 已接 `/api/auth/fusion/token`、`/api/auth/fusion/login`、`/api/auth/sms/send`、`/api/auth/sms/login`、`/api/chat/stream` 用户级频控、`/api/app/logs`、`/api/support/messages` 和 `/upload` 的分布式限流；fusion token / fusion login 按 IP hash 限流，短信发送按手机号 hash + IP hash 和 IP hash 两层限流，短信登录按手机号 hash + IP hash 限流，主聊天、App 自动日志、帮助与反馈用户发消息和上传按 `user_id` hash + IP hash 或 `user_id` hash 限流，不保存明文手机号、验证码、verify token、auth token、聊天正文、反馈正文、图片内容或图片 URL。App 自动日志 attrs 现在 Android 端和后端都会按敏感 key 和敏感 value 二次过滤，丢弃 `phone / token / url / uri / body / message / content` 等字段名对应的值，也会丢弃普通字段名里包含 URL、token、AccessKey、手机号等敏感文本的值；图片上传 DEBUG 日志也只打印脱敏 URL 和响应长度，不打印完整上传 URL 或响应 body。主聊天内容、会员资产、额度、归档、摘要和订单仍以 MySQL / 现有主链为真相，不依赖 Redis。
- Android 聊天页在后端历史模式下不再把本地 30 轮聊天窗口作为首帧同步启动数据：`ChatScreen.kt` 首次组合先用空本地快照放出页面壳，随后在 `LaunchedEffect` 内异步读取本地快照并等待远端 `/api/session/snapshot`。远端快照成功时仍以后端历史为主，并把本地失败消息 / 待恢复用户尾巴并回去；远端快照失败时不再把普通本地历史窗口整段回灌，只保留本地 pending 图片发送和失败态恢复入口，避免后端暂时不可达时把旧 `chat_ui_cache` 显示成 UI 回退。删除所有历史对话后还会用本次清除 epoch 拦截并发 hydrate、前台图片上传回调、pending 图片恢复、queued mainHandler 旧回调、本地窗口 / streaming draft / composer draft 延迟保存，后台 Worker 开流期间也会复查 pending 是否仍存在，避免旧快照、旧图文或旧回复在清空 UI 后晚回来覆盖当前 clean-state；后端同时用 `session_generation` 拦截清空后才晚到的旧 `/api/chat/stream` 请求，一旦用户存在清空代际，缺失 generation 的旧请求会被按 stale 拒绝，清空前已完成的同 `client_msg_id` 也不会通过 ledger replay 幽灵回放；Android 本地聊天窗口、streaming draft、composer draft 和 WorkManager pending 图文也会记录 generation，不匹配则丢弃
- Android 普通输入框草稿当前会写入本机 `chat_ui_cache` 的 `composer_draft_*` 键：用户切 App、锁屏或后台被系统回收后，未发送文字下次进入仍会回填；发送成功 / 清空运行时会清掉该草稿。图片缩略图暂不做跨进程草稿恢复，避免本地 URI 权限和临时文件生命周期带来脏状态
- 主对话每轮都会由后端注入当前时间、用户地点和地点可信度：`chat.go` 组装 `当前时间：yyyy-MM-dd HH:mm:ss（Asia/Shanghai）；用户地点：...；地点可信度：...`。历史轮次当前也会携带 `created_at / region / region_source / region_reliability`，进入模型上下文时以前缀“历史轮次时间：...”和“历史轮次地点：...”注入，让模型知道这轮距离当前大概多久、当时大概在哪里；前端暂不显示每条消息时间戳或地点条。Android 已接入 `ACCESS_COARSE_LOCATION / ACCESS_FINE_LOCATION` 可选定位权限：进入聊天页后请求一次权限，用户授权后每次发送问诊前尽量短窗口刷新系统定位，并用系统 Geocoder 反查为省 / 市 / 区县等地区文本，通过 `X-User-Region`、`X-Region-Source=gps`、`X-Region-Reliability=...` 传给后端；2 小时内的系统定位或短窗口网络定位标记为 `reliable`，只能拿到更旧系统缓存或历史缓存时仍可传地区文本但降级为 `unreliable`，避免把过期城市当成可靠 GPS；App 不上传经纬度，不保存轨迹。生产 Nginx 已透传 `X-Real-IP / X-Forwarded-For`，Go 服务 `GetClientIP` 可拿真实客户端 IP 用于限流、脱敏日志和地区推断；未授权、定位失败或未传 `X-User-Region` 时，`ResolveRegionByIP` 会接入免费离线 `ip2region` xdb 粗定位，优先读取 ECS 本地 `IP2REGION_V4_XDB_PATH`（兼容 `IP2REGION_XDB_PATH`）指向的 v4 库，把公网 IP 转成省 / 市级 `region_source=ip`、`region_reliability=unreliable` 参考，不走收费 API、不放 RDS、不把完整 IP 注入模型。库未配置、私网 IP、代理漂移或查询失败时仍兜底“未知 / unreliable”。天气 API 暂不接入，后续只在确有强需求和成本预算时再评估
- 当前所有真实模型调用统一显式设置 `temperature=0.8`：主对话 `qwen3.5-plus`、B 层通用短期记忆 / C 层长期通用记忆 `qwen3.5-flash` 和今日农情 `qwen3.5-plus` 生成都引用后端同一个 `unifiedModelTemperature` 常量；`top_p / max_tokens / frequency_penalty / presence_penalty` 等其他采样参数暂不显式设置，继续走模型服务默认值。主对话仍是流式、关闭思考、可联网但不强制搜索；B 层通用短期记忆 / C 层长期通用记忆仍是非流式、关闭思考、不联网；今日农情仍是原生 Generation、关闭思考、强制联网搜索
- 后端模型 Key 池已支持 2 到 3 个独立账号 Key 槽位：`DASHSCOPE_API_KEY_1/2/3`，并继续兼容旧 `DASHSCOPE_API_KEY` 和 `DASHSCOPE_API_KEYS` 逗号 / 分号 / 换行列表；配置会自动去重。默认 `DASHSCOPE_KEY_SELECTION_MODE=auto`（留空也按 auto）时，平稳期仍按顺序主备使用，`DASHSCOPE_API_KEY_1` 是首选主 Key，`DASHSCOPE_API_KEY_2` 可作为副 Key；当短窗口内模型请求量达到阈值，或开流前出现限流 / 鉴权类 failover，后端会自动进入一段请求级轮询分流窗口，默认 10 秒内 20 次请求触发、持续 60 秒，窗口结束后自动回到主 Key 优先，不需要为切模式手动改环境或重启。主对话、B/C 摘要和今日农情共用该 Key 池；如果模型请求打开阶段返回 `401 / 403 / 429` 或带限流 / quota 语义的 `400`，后端会在流开始前切下一把 Key，并把触发限流的 Key 短暂冷却，默认 1 秒，可用 `DASHSCOPE_KEY_COOLDOWN_SECONDS` 调整。一旦主对话 SSE 已成功打开，不在同一条回复中途切 Key，避免半条回复和重复成本。阿里云百炼模型限流按主账号下所有 API Key 调用总和计算，所以同一阿里云主账号多 Key 不扩真实模型并发；要扩容必须使用不同主账号 Key。联网搜索另有官方 15 RPS 主账号级限制，超过时 API 不报错但搜索链路不触发，主聊天会自然退成未联网回答；短期可用朋友账号兜底，长期生产建议收回到自己可控账号和账单体系
- Go 后端数据库连接池当前可通过环境变量配置：`MYSQL_MAX_OPEN_CONNS` 默认 10，`MYSQL_MAX_IDLE_CONNS` 默认 10 且不会超过 max open，`MYSQL_CONN_MAX_IDLE_SECONDS` 默认 300，`MYSQL_CONN_MAX_LIFETIME_SECONDS` 默认 1800。后端部署前不做盲目性能调参；首版按 RDS 规格、后端实例数和真实监控再调整。当前高并发巡检结论已沉淀到 `docs/runbooks/pre-server-feature-audit.md` 和 `docs/runbooks/scaling-readiness.md`：Go 语言本身不是当前瓶颈，首版单实例可跑早期；多后端实例前已完成 OSS、迁移锁、主聊天单流 MySQL 租约、主聊天 Redis 限流和 Go 请求级结构化日志等准备，仍需补 B/C 摘要 running guard 的 Redis / MySQL lease、滚动发布脚本和 SLS / 跨实例集中日志
- 基础设施当前主线是 ECS + RDS MySQL + Redis + OSS：ECS `i-2ze5nrem0jrchln4f0eh` 位于北京可用区 L，公网 IP `39.106.1.151`，Go 服务通过 systemd slot 运行在本机 `3000 / 3001` 之一并由 Nginx 代理，当前 active upstream 为 `127.0.0.1:3001`；RDS MySQL 实例 `rm-2zes3vmj76p85n8g1`、Redis 实例 `r-2zet46zvmoo9wu3bic` 和 OSS Bucket `nongjiqiancha-prod` 均在生产 VPC 内可用。阿里云 DNS 已创建 `api.nongjiqiancha.cn`、`nongjiqiancha.cn`、`www.nongjiqiancha.cn` 到 `39.106.1.151` 的 A 记录。2026-06-05 截图确认网站 ICP 备案已通过：主体备案号 `京ICP备2026031728号`，网站备案号 `京ICP备2026031728号-1`，网站名称“农技千查”，域名 `nongjiqiancha.cn`；2026-06-05 20:03 左右 App 备案订单已提交阿里云初审，订单号 `2036780517515`，页面提示预计 2026-06-07 20:00 前审核，需注意接听阿里云备案审核电话；2026-06-07 网站公安联网备案已提交到全国互联网安全管理服务平台，当前待审核，公安备案号未下发，App 公安备案待 App 备案通过 / 正式信息齐后再补；公安备案数据码不写入仓库。2026-06-05 已通过 Let’s Encrypt / certbot 为 `api.nongjiqiancha.cn` 配置 Nginx 443 HTTPS，公网 `https://api.nongjiqiancha.cn/healthz` 返回 200；2026-06-06 已把 API HTTP 80 改为只保留 ACME challenge、其他请求 301 跳 HTTPS，不再直接反代业务 API；2026-06-06 已为 `nongjiqiancha.cn` / `www.nongjiqiancha.cn` 配置 Nginx 静态站和 HTTPS，公网根域名 HTTP 会跳转 HTTPS，HTTPS 返回官网首页 200。2026-06-05 已通过 Cloud Assistant 写入 DashScope 主 / 副模型 Key 槽位、`DASHSCOPE_KEY_SELECTION_MODE=auto` 和自动轮询阈值并重启服务，ECS `/healthz` 显示 `bailian=ok`；真实 Key 值不进入仓库、文档或聊天记忆。真实 App 公网闭环仍待 App 备案管局通过、网站公安备案通过 / 公安备案号、App 公安备案和真机登录 / 主聊天 / 图片问诊回归。ECS `/healthz` 当前应保持 `ok=true / auth_strict=true / bailian=ok / dypns=ok / dypns_fusion=ok / dypns_sms=ok / redis=ok / upload_storage=oss`
- 2026-06-06 资源容量巡检结论：当前 ECS / RDS / Redis / OSS 足够备案等待期、真机联调、早期内测和小流量上线，暂不需要立刻升配；ECS 2 核 4G 实时负载约 0、可用内存约 2.9GiB、系统盘 79GiB 已用约 5.0GiB（7%），RDS 1 核 2G / 50GiB 磁盘约 3.0GiB、近 15 分钟 CPU 约 0.7%、内存约 11%、连接约 0 到 1，Redis 256MiB 当前内存约 4.4MiB、CPU 0 到 0.4%、连接使用约 0.03% 到 0.05%，OSS Bucket 当前 0MB。容量 / 到期 / 升级提醒阈值已新增到 [resource-capacity.md](D:/wuhao/docs/runbooks/resource-capacity.md)，后续 Codex 通过 CLI 或监控发现接近阈值时必须提前提示用户升级、续费或购买资源包
- 主对话锚点真源仍是 `server-go/assets/system_anchor.txt`，当前版本已按“农技千查”产品名收口：强调当前轮优先，历史 / 摘要 / 长期记忆 / 联网只作参考且不是定论；同一作物 / 同一地块可承接仍有效信息，新问题不能直接套旧判断；地点可信度为 `unreliable` 或未知时，地区、气候和农时只能作低可信背景；普通问候 / 寒暄简短接住并引导回作物、症状、图片、地区或具体问题，不套农业诊断格式；信息不足时严禁直接下定论，必须列 2 到 3 种可能性并追问 1 到 2 个关键问题；图片先客观详细描述再分析判断；混配、浓度、倍数、亩用量、兑水量、面积换算应先核对关键参数，再给出计算或使用建议；会员和 App 使用问题只回答已明确的固定入口和公开规则，会员中心可查看档位 / 剩余次数 / 权益，设置里有“帮助与反馈”“检查更新”，但不编造个人账户、订单、扣费或权益异常信息；商业相关内容不做推广、导购、背书或诋毁，但允许按用户要求做相关信息整理、标签解读、技术分析或中立比较；联网只在强时效、强客观核对、用户明确要求、疑难复杂问题或高风险判断需要校准公开权威信息时触发，同轮最多一次，且不能代替关键参数追问；证件 / 登记 / 备案 / 审定类只给疑点和查询方法，不做真伪裁决。B 层提示词已改为通用短期记忆口径，C 层提示词已改为长期通用记忆三块文本口径，且要求多作物、多地块、多棚室、多农资或多事件时分清对象，避免混写
- 当前产品策略倾向已记录为待决策：C 层当前先用 `c_summary` 文本承载三块内容：“长期通用记忆 / 用户画像 / 农业相关重点事件记忆”；当前不新增独立农事事件表，不额外增加 Flash 调用频率。后续若三块文本承载不够，再评估结构化字段或独立农事事件表
- 后期整体运维协助入口已沉淀到 `docs/runbooks/operations-blueprint.md`：范围覆盖 Android 整体 App、Go 后端 `server-go`、RDS / 日志 / 成本 / 发布回滚、帮助与反馈、礼品卡、会员和统一管理后台。当前本机已安装阿里云 CLI，默认 Region 为 `cn-beijing`，可通过 OpenAPI 读取 ECS、RDS、OSS 资源包和 SLS 开通状态等云资源，也可通过 Cloud Assistant 执行 ECS 运维命令；旧 SAE demo 应用已删除。真实 AccessKey、数据库密码、模型 Key、服务端密钥和公安备案数据码不写入仓库、聊天记忆或文档。`api.nongjiqiancha.cn` HTTPS、根域名官网 HTTPS 和管理后台 HTTPS 已落地；Go 后端已补 `X-Request-Id`、`http_request`、`http_request_slow`、`http_request_error` 请求级日志和 [query-ecs-logs.ps1](D:/wuhao/scripts/query-ecs-logs.ps1) 只读查询脚本；农技千查专用 SLS Project `nongjiqiancha-prod-1159547719787456` 已创建，Logstore `server-go` / `nginx-error` TTL 7 天，ECS Logtail 已连接机器组并采集 `/var/log/nongjiqiancha/server.log` 和 `/var/log/nginx/error.log`，查询入口为 [query-sls-logs.ps1](D:/wuhao/scripts/query-sls-logs.ps1)；管理后台已按 Vite 静态前端 + `server-go` 管理 API 落地并部署到 `https://admin.nongjiqiancha.cn/`，详细页面设计见 [admin-dashboard-design.md](D:/wuhao/docs/runbooks/admin-dashboard-design.md)，架构决策见 [ADR-0004-admin-backend-architecture.md](D:/wuhao/docs/adr/ADR-0004-admin-backend-architecture.md)；App 生产 API 真机回归和下载域名入口落地后，必须继续回填 deploy / rollback / logs / db / app-update 等 runbook
- 本机新增只读生产就绪检查脚本 `scripts/check-ecs-readiness.ps1`：通过 Cloud Assistant 检查 `nongji-server`、Nginx、HTTPS healthz、关键环境变量 set/missing/empty、本机上传目录和端口监听，输出脱敏状态，不打印真实密钥。2026-06-07 脚本已收紧为 active upstream slot 不 active、HTTPS healthz 非 200 或关键生产 health 标记缺失时直接失败；关键标记包括 `ok=true`、`auth_strict=true`、`bailian=ok`、`dypns=ok`、`dypns_fusion=ok`、`dypns_sms=ok`、`dev_order_endpoints=false`、`redis=ok` 和 `upload_storage=oss`，避免短信 / 一键登录配置坏了或开发订单入口误开时被误判成通过；最新检查显示 active upstream 为 `3001`，`nongji-server-3001 active/enabled`、Nginx 配置 OK、HTTPS healthz 200、`bailian=ok`、`dypns=ok`、`dypns_fusion=ok`、`dypns_sms=ok`、`redis=ok`、`upload_storage=oss`，且 `LOG_FILE_PATH`、`ACCESS_LOG_SLOW_MS`、`DASHSCOPE_KEY_SELECTION_MODE` 与自动轮询阈值均为 set；仍缺 App 备案通过、网站公安备案通过 / 公安备案号、App 公安备案和一键登录 / 主聊天真机回归
- 2026-06-06 最新后端已通过 `scripts/deploy-ecs-server.ps1` 迁移为单 ECS 双端口发布，当前 Nginx active upstream 为 `127.0.0.1:3001`，`nongji-server-3001.service` active/enabled，旧 `nongji-server.service` 已 disabled。当天巡检曾发现多个 `nongji-drain-stop-*` transient 排空任务叠加，导致 active slot 被误停并短暂 502；已手动启动当前 slot 恢复公网 200，并把部署 / 回滚脚本改成每次先清理旧 drain 任务，再安排新的排空。脚本当前会使用远端互斥锁、非当前 slot 健康检查、生产 HTTPS healthz 断言、Nginx 切换失败自动恢复和旧 slot 延迟停止，减少单进程重启造成的 502 空窗；这不等于多 ECS 容灾。生产 readiness 复查为 Nginx OK、HTTPS healthz 200、`bailian=ok`、`dypns=ok`、`dypns_fusion=ok`、`dypns_sms=ok`、`redis=ok`、`upload_storage=oss`，未读取或打印任何真实密钥值
- 2026-06-06 已补免费优先安全加固：安全组公网入站已撤销 `TCP 22 / 0.0.0.0/0`，ECS 本机 `ssh` 服务也已停用并 disabled，当前只对公网放行 `80 / 443` 和 ICMP；阿里云云安全中心免费版已显示 ECS agent online、风险计数 0；Nginx 已补全局安全头兜底 `/etc/nginx/conf.d/nongjiqiancha-security.conf`，官网部署脚本也会写入 CSP / HSTS / Permissions-Policy 等静态站安全头；API HTTP 80 已改为 ACME challenge + 301 HTTPS 跳转；后端内部 secret 入口已补 Redis / IP 频控，主聊天 `client_msg_id` 已补 128 长度上限。安全加固入口见 [security-hardening.md](D:/wuhao/docs/runbooks/security-hardening.md) 和 [security-threat-model.md](D:/wuhao/docs/runbooks/security-threat-model.md)，当前不先购买付费 WAF / 云防火墙 / 高防，真实放量或持续攻击时再按日志和成本判断
- 下一阶段上线推进计划已沉淀到 `docs/runbooks/go-live-plan.md`：当前顺序是先定死 App 名称 / 图标 / 包名 / 签名 / 协议 / 软著材料，买域名和中国内地云资源后立即启动 ICP / App 备案；网站 ICP 已通过，App 备案已提交阿里云初审但尚未通过；手机号登录、后端部署、RDS、OSS、SLS、帮助与反馈、检查更新、模型 Key 池和真机联调在备案等待期间并行推进；网站 ICP 通过后已补 `api.nongjiqiancha.cn` HTTPS，并已提交网站公安联网备案，后续继续跟进网站公安备案审核 / 公安备案号、App 备案编号、App 公安备案和应用商店物料；不等手机号登录全部完成才开始备案，也不提前硬做完整管理后台
- 官网首版代码已新增到 `site`，使用 Vite 静态站实现，当前是克制暗色一页展示：左侧品牌锁定与主张“看清作物问题”，右侧三项能力“原生视觉感知 / 多模态线索融合 / 稳健农技推理”，安卓版下载按钮和 footer 公司主体 / ICP；不做顶部导航、对话演示、管理后台或复杂营销模块，不公开点名 `Qwen3.5-Plus`，也不把“联网校准”单独包装成公开核心卖点。构建时可通过 `VITE_ANDROID_APK_URL` 注入真实 APK 下载地址，只有合法 https `.apk` URL 才启用下载按钮，未配置时按钮不可点击且不展示内部“准备中”流程。footer 展示公司名“北京农技千问科技有限公司”和网站 ICP 备案号 `京ICP备2026031728号-1`，并链接工信部备案系统；网站公安备案号未下发前公开官网不展示“待补充”占位，不伪造编号；2026-06-06 已通过 `scripts/deploy-ecs-site.ps1` 部署到 `https://nongjiqiancha.cn/` 和 `https://www.nongjiqiancha.cn/`，根域名证书路径为 `/etc/letsencrypt/live/nongjiqiancha.cn/`，有效期到 2026-09-04。官网运维入口见 `docs/runbooks/official-website.md`
- 软件著作权登记按 `农技千查 Android应用软件 V1.0` 快照口径准备，和 ICP / App 备案、前后端继续开发并行推进；本机桌面 `D:/Desktop/农技千查软著材料` 已生成申请信息草稿、提交步骤简表、操作说明书 docx、Android 客户端源代码前后各 30 页摘录 docx 和阅读版 txt。软著账号、企业实名认证、申请表确认、盖章、营业执照和经办授权仍需公司或授权经办人处理；仓库入口见 `docs/runbooks/software-copyright.md`
- 上线前功能巡检已开始按功能记录到 `docs/runbooks/pre-server-feature-audit.md`；当前已完成会员中心 / 额度体系、Go 后端高并发 / 性能边界、帮助与反馈、礼品卡、检查更新、服务协议 / 隐私政策 / 风险提示、主聊天与图片发送、B/C 记忆与模型调用、今日农情、账号 / 手机号登录与生产鉴权、统一管理后台、支付真实接入十二块。会员/额度当前可作为“占位展示 + 后端真相”继续推进，但真实收费前必须先接账号 token、生产 `AUTH_STRICT=true`、真实支付回调验签和对账；买服务器本身不等于会员可以直接开卖。帮助与反馈当前主链可过早期内测，后台网页首版已补帮助与反馈会话 / 回复入口，但公开运营前还要继续完善工单状态、搜索、分配和消息保存 / 删除规则。礼品卡后端、Android 和管理后台已接入首版批次创建、卡状态、未兑换卡作废、兑换尝试和用户侧兑换接口；Android 设置页只在 `/api/gift-cards/redeem` 返回成功后展示“兑换成功”并刷新会员权益，没有后端成功结果时不能假弹真实成功。检查更新当前主链是自有服务器 APK 分发，后端支持可选 `APP_ANDROID_APK_SHA256`，Android 下载后会先校验最终 https、文件大小、SHA-256、包名和 `versionCode`，再调起系统安装页，客户端另有 200MB 默认下载硬上限。协议与隐私当前口径见 `docs/runbooks/legal-privacy.md`，真实服务商落地后必须按实际权限、SDK、数据保存和账号权利入口复核。主聊天与图片发送当前没有发现旧直连模型、旧完成接口、旧 active-zone、旧图片手势或旧上传通道并存；部署后重点验证公网 https 图片链、WorkManager 恢复、弱网多图、删除历史活跃流 409，以及首版单实例 / OSS 取舍。B/C 记忆当前没有发现旧摘要接口或 Android 直连摘要模型参与主链，失败会保留 `pending_retry_b/c`，写回按 `round_total` 防旧快照覆盖；多实例前需要补摘要数据库 lease 或明确单实例观察。今日农情当前是后台定时生成 + 用户只读缓存的独立卡片，不进入 A/B/C、不扣问诊次数、不在用户打开 App 时临时调模型；部署后需要配置 `DAILY_AGRI_JOB_SECRET`、定时任务、SLS 告警和后台状态页。账号 / 手机号登录已有首版后端账号表、v2 session token、短信 / 融合认证接口、Android 登录页和阿里云融合认证 SDK；公开生产仍必须完成一键登录真机回归，且上线前轮换已暴露过的主账号 AccessKey；Redis 分布式认证限流已接入，不能把共享静态 token / 测试用户 ID 当正式登录方案。统一管理后台第一版代码已落地并部署到 `https://admin.nongjiqiancha.cn/`，形态为 Vite `admin` 前端 + `server-go` `/admin-api/v1/*`，覆盖登录、总览、监控面板、用户、会员额度、订单、礼品卡、帮助反馈、App 日志、今日农情、检查更新、审计和服务健康；Nginx 已静态托管后台前端并同域反代 `/admin-api/`，后台 HTTPS 证书已签发，owner 账号已通过一次性 bootstrap 初始化且 ECS `ADMIN_BOOTSTRAP_*` 已清理，公网已验证首页、登录和总览 API。支付当前没有真实渠道、SDK、自动续费、退款或对账；真实接入必须走服务端创建订单、支付渠道回调验签、幂等发权益和对账，不允许打开开发期直改接口当正式收费
- 会员有效等级由后端按 `tier_expire_at` 实时计算：Plus / Pro 到期后对 `/api/me`、每日额度、加油包购买资格、Plus 升 Pro 入口都按 Free 处理；若数据库出现 Plus / Pro 但 `tier_expire_at` 为空，也按 Free 处理，避免脏数据变成永久会员；Plus 升 Pro 时仍会把 Plus 剩余额度折成永久升级补偿次数，Pro 到期后未用完的升级补偿 / 加油包仍可按当前消耗顺序继续用于超额请求；后端扣减加油包时按扣减前已锁定的剩余次数判断状态，只有扣完最后 1 次后才把该加油包标为 `used_up`
- 管理后台已补首版监控面板：`GET /admin-api/v1/monitoring` 聚合服务健康、今日 / 24h / 7d 使用情况、App 自动日志错误、待回复反馈、今日农情、礼品卡兑换异常、后台操作失败、地区分布和 App 错误 Top，并返回 `action_items` 与 `capabilities`；前端改成“整体状态 / 先处理事项 / KPI / 服务状态 / 后台能力状态 / 明细表”的简洁视图，左侧导航明确区分可用、只读和后续报表，点击菜单会保留侧栏滚动位置，品牌占位已换成绿叶图标。该面板仍不是完整 SLS 告警中心，发布 / 回滚、支付订单、登录精准漏斗和高风险批量操作后续再接。
- Android 端右上角黑色线性“圆角方框 + 加号”当前作为会员中心入口并已接入首版会员中心底部面板：打开时读取 `/api/me` 的 `tier / tier_expire_at / daily_remaining / topup_remaining / upgrade_remaining`，并用本地 request epoch 防止重叠刷新时旧慢请求覆盖最新会员 UI 状态。后端 Plus / Pro 会员周期当前为 30 天。面板标题下不再显示“次数、追问记忆和加油包”副文案；顶部只用一条轻量状态条展示今日剩余额度和当前档位，Free 只显示“基础额度”，不显示到期日期；Plus / Pro 显示到期日期；未同步或读取中时只显示“未同步 / 读取中”，不重复显示“未连接”，且套餐开通按钮置灰显示“同步后开通”，不允许在会员真相未同步时启动开通。若存在升级补偿次数或加油包次数，会在“套餐”标题下方同一行以胶囊展示“升级补偿次数 x次 / 加油包 x次”；Free 用户如果是 Plus / Pro 到期后仍剩升级补偿或加油包，也照样展示并可按后端消耗顺序继续使用。套餐区不再单列免费版大卡，直接展示 Plus / Pro 付费套餐，并在套餐区顶部明确提示“会员购买暂未开放；当前页面只展示套餐规则，不会发起扣费或变更权益”；当前同档套餐按钮置灰显示“当前套餐”，Pro 用户看到 Plus 卡片时显示“当前为 Pro”，其他开通 / 升级按钮当前都置灰显示“暂未开放”。套餐卡片用户可见文案不展示具体 A 层轮数，只展示 Plus 25 次 / Pro 40 次、图文问题、记忆与上下文、复杂问题推理和多作物多地块复盘等产品化权益表达。Plus 升 Pro 规则在底部卡片里展示“升级 Pro 后，Plus 剩余权益会自动折成补偿次数。”；加油包文案明确 `6元/80次`、仅 Plus / Pro 可订购、永久有效、用完再续；若 `/api/me.topup_remaining > 0`，Plus / Pro 按“用完再续”置灰，Free 到期后仍有加油包余额则显示“剩余次数可用”，避免暗示 Free 可直接续订；无可用加油包时订购按钮同样置灰显示“暂未开放”或资格提示。当前支付功能尚未接入，主界面会员底部面板和设置会员子页不会调用后端订单接口；后端开发期订单变更接口默认返回 `PAYMENT_NOT_CONFIGURED`，只有显式设置 `ALLOW_DEV_ORDER_ENDPOINTS=true` 且当前环境明确为 `local / dev / development / test` 时才允许本地调试，缺失环境名也关闭；真实支付成功后的统一收口 UI 仍只预留在主界面会员底部面板，显示中间黑底白字卡片“订购成功 / 确定”，确认后会刷新 `/api/me` 同步会员中心数据，并在刷新到可用额度 / 补偿 / 加油包后清掉当天“次数用完”的本地发送锁。套餐到期暂不做主动提醒，到期后由后端自动按 Free 权益计算
- 当前成本核算口径已按真实代码重算：主对话是实时 SSE `qwen3.5-plus`，不吃 Batch Chat 半价；按当前公开价输入约 0.8 元 / 百万 tokens、输出约 4.8 元 / 百万 tokens 估算。`0.018 ~ 0.019 元 / 轮` 不再称为最保守成本，只作为偏重使用场景估算的一部分；当前项目记忆采用三档：真实平均约 `0.008 ~ 0.015 元 / 轮`，偏重使用约 `0.018 ~ 0.022 元 / 轮`，连续 4 图 / 上一轮图片 / Pro 满 A 层 / 长输出 / 搜索等极重场景约 `0.025 ~ 0.030 元 / 轮`。Plus / Pro 会员阶梯暂不调整；加油包调整为 `6元/80次`，同一时刻仍只允许 1 个 active 加油包。后续上线后应记录每轮实际 `input_tokens / output_tokens / search` 再校准会员毛利。
- Android 左上角汉堡按钮当前已接入首版简洁设置页：使用独立 `HamburgerMenuSheet.kt`，不做头像、不清空聊天、不接多会话历史。页面顶部展示居中“设置”标题，并保留左上角返回按钮和设置入口组；入口包括“会员中心 / 账号管理 / 帮助与反馈 / 检查更新 / 礼品卡 / 服务协议 / 当前账号”，设置首页入口行、图标、文字、分割线和卡片圆角已调到适中一档：主设置卡片使用白底、浅边框、内缩分割线和右侧轻箭头，首页外侧边距收窄以放大卡片本体，同时保留足够的行高和图标 / 文字重量，避免灰块过重或整体过小。其中主界面右上角“会员中心”仍打开现有底部面板；设置页内的“会员中心”不再关闭汉堡页或弹底部面板，而是进入右进左出的会员中心子页，复用同一套会员内容和 `/api/me` 数据，短 ID 收进“会员中心”标题行展示，左上角返回和系统返回键都会先回到汉堡菜单页；“帮助与反馈”现在进入右进左出的站内消息子页，会从后端拉取同一用户的帮助与反馈历史，用户可发送反馈，后台 / 系统新消息会让设置页“帮助与反馈”行右侧显示小红点，进入帮助与反馈页并成功同步后只把本次已拉取到的最后一条后台 / 系统消息及更早消息标为已读，避免加载和已读之间新到的消息被误清红点；“检查更新”按自有服务器 APK 分发接入 `GET /api/app/update`，无更新提示“已是最新版本”，有新版本弹“发现新版本”卡片，按钮为“稍后 / 立即更新”，立即更新会下载后端返回的 https APK 并调起 Android 系统安装确认，debug-only 预览面板已补“检查更新”卡片预览；“礼品卡”放在“检查更新”下方，主设置页入口只显示“礼品卡”三个字；“服务协议”和帮助与反馈、检查更新、礼品卡合并在同一个设置卡片组里，不再单独占一个卡片；礼品卡不再带副标题，可进入右进左出的轻量子页，输入框边框为纯黑，输入框和兑换按钮放在页面中部形成上下两行结构，兑换按钮字体加大，但不展示占位提示或说明文案，前端不限制字符类型、大小写或长度；礼品卡入口图标按设置页统一视觉放大到同一 24dp 图标槽内约 74% 宽的线性礼物盒；Android 礼品卡页已经接到后端 `/api/gift-cards/redeem`，输入卡码后调用真实兑换接口，成功才展示“兑换成功”卡片并刷新会员权益，失败按后端错误展示“卡码不正确 / 已使用或作废 / 已过期 / 档位较低 / 频率过高”等用户可懂文案；前端不保存完整卡码，也不在没有后端成功结果时假弹真实成功；debug-only 预览面板保留“礼品卡兑换成功”卡片样式，卡片下方有“确定”，点击后关闭；礼品卡内容为空时兑换按钮是禁用态，输入完成后支持键盘 Done 触发同一兑换；“账号管理”现在进入右进左出的轻量子页，放“手机号 / 删除所有历史对话 / 退出设备 / 注销账号”四项，手机号显示脱敏号码或未登录；删除所有历史对话会先弹“取消 / 确定”二次确认，确认后调用 `POST /api/session/clear`，后端删除当前用户 `session_ab` 里的 A 层滑窗、B 层通用短期记忆、C 层长期通用记忆和 `session_round_archive` 30 天 UI 归档，前端成功后清当前聊天 UI、本地聊天快照、草稿、streaming draft、待发送 WorkManager 任务和私有 composer 图片；该操作不删除会员、额度、加油包、礼品卡、帮助与反馈、`quota_ledger` 或本机 `user_id`，若当前有活跃生成流则提示稍后再删除；手机号会显示当前登录状态；退出设备入口会调用 `POST /api/auth/logout` 吊销当前设备 session，成功后清本地 auth token 并回到登录门；注销账号仍未开放，不再展示“后续接入”式半成品说法；这些二级子页的左上角返回和系统返回键都会先回到汉堡菜单页，只有菜单首页的返回才关闭汉堡页；当前退出设备不清空本机 `user_id`、聊天归档或会员资产，只结束当前设备登录态，不影响聊天滚动链
- “服务协议”当前已从设置页平铺的“服务协议 / 隐私政策 / 风险提示”三行改为一个二级目录，点设置页“服务协议”后进入目录页，再进入“用户协议 / 隐私政策 / 第三方信息共享清单 / 个人信息收集清单 / 应用权限 / 风险提示”；这些页面都是 Android 本地内置正文，不需要联网加载，左上角返回和系统返回键会先从正文回到服务协议目录，再回到设置菜单。协议主体暂按用户提供的“北京农技千问科技有限公司”和 `465989879@qq.com` 填写。6 个页面已在 2026-06-01 按手机号一键登录 SDK 接入复核：隐私政策、第三方信息共享清单、个人信息收集清单和应用权限已加入阿里云融合认证 SDK、电话状态、网络状态 / Wi-Fi 状态、手机号认证、短信验证码、本机系统定位、登录账号标识、认证 token 和脱敏手机号说明；2026-06-05 又按 debug / release merged manifest 补充 WorkManager 合并的后台任务、唤醒、开机后恢复调度和前台服务相关权限说明，这些权限只用于带图消息在 App 切后台、进程被系统回收或设备重启后按同一条待发送任务有限重试，不用于 App 外推送、广告通知、通讯录或短信读取。定位权限只用于问诊地区上下文校准，App 不上传经纬度、不保存轨迹、不接地图 SDK。本版本不提供真实支付、自动续费或农资商品交易，礼品卡仅支持官方卡码兑换会员权益；不把未来业务写死为永不开展，也不虚构当前已经接入支付 SDK、农资商品交易或 SLS 业务日志；Redis 用于手机号认证、主聊天用户级频控、App 自动日志接收、帮助与反馈用户发消息和上传短期限流，不在 App 文案里单独包装成业务功能，且主聊天频控只保存 user_id hash，不保存聊天正文或图片内容。照片走系统 Photo Picker，拍照走外部相机 + FileProvider 临时 URI，Android Q+ 拍照成功后会把原始照片另存到系统相册 `Pictures/农技千查`，当前不申请相机权限、相册 / 存储读写权限、录音、通讯录、短信或通知权限，且当前不做 App 外通知 / 推送。后续若公司主体、ICP备案、隐私政策、支付规则、权限清单、第三方服务清单或律师审核意见变化，必须同步改 App 内文案和项目记忆
- 帮助与反馈首版以后端 `support_messages` 为真源：用户侧接口包括 `GET /api/support/summary`、`GET /api/support/messages`、`POST /api/support/messages`、`POST /api/support/read`，均走当前用户鉴权；`POST /api/support/messages` 默认按 `user_id + IP` 做 10 分钟 20 条短期限流，配置 Redis 时跨进程共享，未配置 Redis 时回退本进程限流。后端会在用户首次反馈或距上一条通用反馈超过 24 小时时写入 `sender_type=system` 固定自动回复并随响应返回 `auto_reply`；自动回复不调用模型、不承诺 SLA、不替代后台人工回复。当前逻辑已收成简单兜底版：短问候请用户说明问题并提示客服会在本页跟进；纯图片或“见图 / 看图”提示已收到图片并请补充具体问题；其他内容统一走通用回复，说明本页主要处理 App 使用反馈、后续回复会在本页显示，农业技术咨询可回主聊天页继续问。后台会话列表的 `needs_reply` 按最新非 `system` 消息判断，自动回复不会盖掉“用户待回复”状态；`POST /api/support/read` 支持 `last_seen_message_id`，Android 进入页后只标记本次已拉取到的最后一条后台 / 系统消息及更早消息，避免未展示的新回复被误读；后台 / 运维侧先提供 `SUPPORT_ADMIN_SECRET` 保护且带 IP 短期限流的 `GET /internal/support/conversations`、`GET /internal/support/messages?user_id=...` 和 `POST /internal/support/messages`，用于后续管理后台读取会话列表、历史详情和推送后台回复；这些内部操作会写入 `admin_audit_logs`，审计只记操作元信息和条数 / 字数 / 图片数量，不记录反馈正文或图片 URL。帮助与反馈消息支持文字和图片附件，图片继续复用主聊天的相机 / Photo Picker / 私有 JPEG 副本 / 压缩上传链，单次最多 4 张，每张上传前按现有图片规则压到 JPEG 且不超过 1MB；帮助与反馈页输入区复用主聊天的底部输入框壳、加号、发送按钮、附件面板和图片预览条，并通过 IME padding 避免键盘遮挡输入框；发送时会在输入区上方显示“正在上传图片...”或“正在提交反馈...”。当前已有网页后台首版，但还没有完整客服工单分配、客服坐席体系、站外推送通知或消息搜索；首版只保证单用户一条站内会话线、会话列表可查、历史可拉取、轻量自动回复、后台可回复、App 内红点可提示未读
- 这里的“完整后台”当前口径已收紧：网页后台首版已经上线；仍未完成的是完整客服工单系统、搜索 / 分配 / 标签、SLS 自动告警、发布 / 回滚写操作和支付正式订单链路。
- App 自动日志当前做最小接收与只读查询骨架：Android 在关键失败点自动 fire-and-forget 调用 `POST /api/app/logs`，后端走现有用户鉴权，写入 `client_app_logs` 表并同步打印结构化服务日志；内部只读查询 `GET /internal/app/logs` 暂复用 `SUPPORT_ADMIN_SECRET` 保护，可按时间、用户、事件名和等级过滤并返回明细 / 聚合摘要；第一版网页后台另提供 `GET /admin-api/v1/app-logs`，走后台账号 session / CSRF / 角色校验。后端已新增最小内部操作审计表 `admin_audit_logs` 和只读查询 `GET /internal/admin/audit-logs`，现有内部 App 日志查询、帮助与反馈内部会话列表 / 读取 / 回复、今日农情内部生成会写审计元信息；可选 `X-Admin-Actor` 只用于内部 secret 入口的审计标签，不等于后台账号鉴权。当前事件覆盖远端历史快照失败 / 解析失败、前台 SSE 中断、后台 WorkManager 流失败、图片上传失败、帮助与反馈发送失败、检查更新失败 / 解析失败；上报和查询内容只包含事件名、等级、短消息、App 版本、Android 系统版本、设备型号和少量结构化错误字段，不上传或返回聊天正文、AI 回复全文、图片内容 / URL、手机号、token 或模型 Key。Go 服务端另有统一请求级结构化日志，非降噪业务请求会记录 request id、路径、状态码、耗时、响应字节数、脱敏 IP、鉴权模式和可选 user_id；不记录 query string、请求 body、Authorization、手机号、图片 URL 或模型 Key，慢请求阈值默认 3000ms；该日志已通过 SLS `server-go` Logstore 接入 7 天保留。Nginx error log 也已接入 SLS `nginx-error`，完整 Nginx access 暂不全量采集，避免高噪声和额外费用。该能力已接入后台监控面板，仍是后续 SLS 告警的前置骨架，不是用户手动上传日志，也不是完整告警中心；排障入口见 `docs/runbooks/app-client-logs.md` 和 `docs/runbooks/logs-sls.md`
- 今日农情首版已接入为独立每日资讯卡片：后端新增 `daily_agri_cards` 表、`GET /api/today-agri-card` 只读接口和 `POST /internal/jobs/today-agri-card/generate` 内部生成接口。生成链路走 DashScope 原生 Generation 协议调用 `qwen3.5-plus`，显式设置 `temperature=0.8`，显式关闭思考模式，强制联网搜索，`search_strategy=max`，`enable_source=true`；服务端用数据库 lease 防并发，生成前会读取过去 7 天已 ready 的今日农情，把标题 / 摘要 / 来源 / 链接喂给模型，要求当天不要重复同链接、同标题或同一事件；服务端也会硬过滤过去 7 天和当天候选里的重复链接 / 重复标题，并只发布 JSON 可解析、严格 3 条、https、近 7 天、来源 URL 来自搜索结果且域名可信的“今日农情”。广告、导购、软文、模型 / 提示词泄露类内容会被过滤，过滤后不足 3 条则不发布新卡片。Android 在后端历史 hydrate 完成后调用 `/api/today-agri-card`，只有 3 条有效 item 才以 `ChatTimelineItem.TodayAgriCard` 插入聊天列表展示层；它不是 `ChatMessage`，不进入本地聊天快照、A/B/C 上下文、`session_ab`、`session_round_archive`、摘要或问诊扣次。同一用户当天只要还没有发送过文字 / 图片 / 失败态消息，今日农情可以展示；当天一旦发送，Android 会按 `date_cn` 规范化出的 `yyyyMMdd` 日期键写入本机 `chat_ui_cache`，让卡片当天永久隐藏，删除历史也不恢复，第二天拉到新日期卡片后再出现；缺失 / pending / failed 时前端静默不展示，不阻塞聊天页；点击单条农情只用系统浏览器打开来源 URL
- Android 构建链当前为 Gradle wrapper 8.13、Android Gradle Plugin 8.13.2、Kotlin Android / Compose Compiler Gradle plugin 2.1.21、WorkManager `work-runtime-ktx:2.11.2`；Compose 编译已使用 `org.jetbrains.kotlin.plugin.compose`，不再使用旧 `composeOptions.kotlinCompilerExtensionVersion`
- 聊天消息运行时当前是**单一正向列表主人**：`ChatRecyclerViewHost.kt` 使用普通 `LazyColumn`，`messages` 仍按 oldest -> newest 存储并直接传给列表，视觉底部最新消息是 `lastIndex`
- 底部 composer 仍是页面底部的独立 UI 宿主，继续负责输入、IME、placeholder、发送禁用与收口视觉；**它不是消息运行时主人**
- `ChatScreen.kt` 当前把消息列表和 composer 作为页面 `Box` 内的兄弟层渲染：列表铺满消息区，composer 用 `align(Alignment.BottomCenter)` 固定在底部。composer 已从旧 `SubcomposeLayout` 测量链里拆出，键盘动画不再每帧拖着列表一起 remeasure；composer 自己继续吃 `imePadding()`，根容器不吃 IME padding，以保持“键盘只移动输入框，不抬升消息工作线”
- composer 内部内容高度不作为聊天列表 reserve 真值。多行文字、当前图片缩略图预览、未来附件缩略图、图文混排等只能影响输入框内部布局 / 内部滚动 / composer 自身视觉高度；聊天列表 bottom padding 只允许吃折叠态 composer 外壳、safe area / navigation bar、发送期锁定 reserve 和工作线 gap。IME 动画只移动 composer，不进入列表 reserve
- Android 端 `+` 入口当前已接入首版图片输入：点击后从底部弹出附件卡片，只开放“相机 / 照片”两项；照片入口优先使用 Android 官方 Photo Picker 按剩余槽位请求选择上限，manifest 同步声明官方 Photo Picker backport module 依赖，支持的系统会在选图阶段限制到本轮最多 4 张，不支持 / 兜底选择器仍由 App 回来后截断并提示。输入框已有 4 张图片时，附件卡片顶部和相机 / 照片点击都统一提示“最多4张图片”。图片选中后会先生成 App 私有 `files/composer_images` 稳定上传副本：若原图已经是 JPEG、`<=1MB`、最长边 `<=1024px` 且 EXIF 方向正常 / 未定义，则直接复制不重新编码；其他可解码图片才走压缩并统一转 JPEG。生成副本后按最新剩余槽位二次截断并进入输入框壳体内的缩略图预览，最多 4 张；缩略图可点开全屏预览，输入框图片预览和聊天区用户图片预览共用 `ImagePreviewPager.kt`，内部使用 Telephoto `ZoomableAsyncImage` + `HorizontalPager`，均支持单击图片关闭、左右滑动切换同组图片、双指缩放、放大后拖动查看细节以及 Telephoto 的边界阻尼 / 嵌套滚动 handoff；单击关闭只在单指轻点且未移动时触发，一旦出现多指缩放或新增第二根手指，会等所有手指都离开后才重新允许下一次单击关闭，避免缩放时误退出预览。旧 `ImagePreviewGesture.kt` 手写手势链已删除。带图时 composer 会增加自身高度和文本输入保底空间，不把图片塞进 `BasicTextField` 光标流。点发送后会先把带本地 `imageUris` 的用户消息上屏并清空输入框，再后台上传；若确认该图片仍是 App 私有 `composer_images` 里的 JPEG 且大小 `<=1MB`，发送时直接复用这份已压缩或直通的字节，不再二次压缩。当前压缩序列为 `1024@Q85 -> 1024@Q80 -> 896@Q80 -> 896@Q70 -> 768@Q70 -> 640@Q60 -> 512@Q60`，极端可解码图片仍超限时继续保持 Q60 等比缩小，直到 <=1MB；全程不裁剪、不拉伸。上传等待态只做一次底部定位，不保留 streaming 发送锚点；上传成功后按用户选择顺序把远端 URL 写入 `SessionApi.StreamOptions.images` 并继续请求模型，上传失败则保留该用户消息并标记发送失败，不再额外弹“图片上传”类浮层；若 App 在图片上传阶段或上传成功但尚未可靠完成远端请求时被系统杀掉，WorkManager 延迟兜底会复用本地稳定副本或已上传 URL 补发；服务端只会在轮次归档成功后向 App 发送 SSE `[DONE]`，额度扣减在归档成功后执行；若 `ConsumeOnDone` 临时失败会按同一 `client_msg_id` 短重试，重复扣由 `quota_ledger` 唯一键防住；冷启动 / 远端 hydrate 会保留仍在后台队列中的图片用户消息，不把它误标成最终失败或从 UI 消失；同一 30 轮窗口里如果有多条仍在 WorkManager 队列中的图片用户消息，前端会把这些 pending 消息一起纳入远端归档恢复轮询，不再只盯最后一条；若失败 assistant 重试时只有本地图片副本没有远端 URL，会先重新上传本地图。App 内相机优先让外部相机写入 App cache 下的 `NongjiFileProvider` 临时 URI；启动外部相机时会给输出 URI 加读写 grant flags、ClipData，并按可解析相机包显式授权，回调或启动失败后撤销授权；导入 App 私有 `composer_images` 成功后，Android Q+ 再把原始拍照结果复制到系统相册 `Pictures/农技千查`；临时文件、拍照取消和相机启动失败都会清理；只有 FileProvider 目标创建失败时才回退到直接创建相册 URI。当前 Photo Picker、外部相机 FileProvider URI 和 Android Q+ 相册复制不额外申请相册 / 相机 / 存储权限；定位权限独立用于问诊地区上下文，不上传经纬度、不保存轨迹。相机待回调 URI、是否相册保存和临时文件路径当前用可保存状态暂存，降低外部相机期间 Activity 重建导致拍照结果丢失的概率。聊天区用户消息在本地副本仍存在时优先用 `imageUris` 预览，远端 `imageUrls` 作为冷启动 / 换机 / 本地副本已清理后的兜底；聊天区用户消息图片缩略图当前为 112dp，并有 12MB LRU 内存缓存减少滑动回看时重复读文件 / 拉远端图 / 解码。前端本地 30 轮 UI 窗口裁剪后，消息已不再引用的 App 私有 `composer_images` 旧 JPEG 会在本地窗口落盘后后台清理，避免长期单对话使用时旧图继续占手机空间；远端 URL 和后端 30 天归档不受影响。图片预览高度只影响 composer / 消息内容内部，不进入列表 reserve / 96dp 工作线
- 后端图片入口同步收紧：`POST /upload` 现在要求同一套用户身份头 / token，只接受单张 `<=1MB` JPEG；服务端必须配置 `BASE_PUBLIC_URL` 或 `UPLOAD_BASE_URL` 为公开 `https` 基地址后才会返回图片 URL；`/api/chat/stream` 会校验图片 URL 必须来自该公开基地址下的 `/uploads/*.jpg`，不再信任客户端任意外部图片 URL，也不再用请求转发头临时推导图片可信域名。Android 上传会给 `/upload` 带 `X-User-Id` 和可选 `Authorization`，仍只上传前端生成的私有 JPEG 副本
- Android 聊天区远端图片缩略图预览单次读取最多 2MiB；超过上限的异常大图会跳过本地缩略图解码，避免冷启动 / 回看远端历史图片时因为异常图片造成内存尖峰。该限制只影响聊天区缩略图预览兜底，不改变上传、全屏预览主链或后端 30 天归档
- Android 主聊天和帮助与反馈已补远端历史图片过期占位：远端 URL 因 OSS 生命周期删除、404 或解码失败时，主聊天缩略图显示“图片已过期”，全屏预览显示“图片已过期，仅保留文字记录”；帮助与反馈图片缩略图显示“已过期”，全屏预览复用同一过期文案。该占位只影响图片展示，不改变文字问答归档、本地图片缓存、上传链、模型上下文或 96dp 工作线
- 图片 pending 远端恢复只用于补旧答案：如果恢复轮询期间用户又发起了新的活跃 SSE，这条补旧答案链不会取消或打断当前新对话流
- SSE 完成态当前以轮次归档为用户可恢复真相：服务端只在 `AppendSessionRoundComplete(...)` 成功写入 ledger / 归档后才发送 `[DONE]`；`ConsumeOnDone` 在归档成功后执行，若临时失败会按同一 `client_msg_id` 短重试，重复扣由 `quota_ledger` 唯一键防住。replay 只恢复已归档答案，不再补扣旧轮次，且会在模型 Key 检查、进行中锁、prompt 组装和额度检查之前优先返回；若首查未完成，后端会先拿同用户 inflight 锁并二次复查完成态，再读取 A/B/C 快照和组装 prompt，降低同一 `client_msg_id` 在完成临界点重复开流的机会。`session_round_ledger` 当前新增 `request_hash`，按用户文本和图片 URL 计算同一轮请求指纹；如果同一 `client_msg_id` 被不同内容复用，服务端会返回 `CLIENT_MSG_ID_CONFLICT`，不再把旧答案误回放给新内容。`/api/session/clear` 与 `/api/chat/stream` 获取同用户 inflight 租约前还会共用 MySQL 用户级命名锁，避免“清空检查无活跃流”和“旧请求刚好开新流”并发穿透；清空历史会递增 `session_generation`，Android 后续 `/api/chat/stream` 会随请求携带当前 generation，服务端在开模型前和归档前校验，不匹配则返回 `STALE_SESSION_GENERATION` 或丢弃归档；用户一旦存在清空代际，缺失 generation 的请求也会按 stale 拒绝，清空前已完成的同 `client_msg_id` 不允许在清空后通过 ledger replay 回放。每日额度按后端上海时区自然日计算，请求开始时记录 `day_cn`，23:59 发起但 00:00 后完成仍扣发起当天，00:00 后新发起的请求走新一天额度。主模型上游开流当前不做服务端自动二次重试，Android 前台流也不对模型开流失败做静默多次重试；WorkManager 只对同一条 pending 图片消息的图片上传失败、网络中断、流异常结束、`409`、限流或临时上游错误做带上限的退避重试，普通可恢复失败最多重试 5 次后移除 pending。`chat_stream_inflight` 获取结果以 lease token 为准，并新增同一用户活跃流唯一约束：同一用户同一时间只允许一条活跃主流式请求，重复 / 并发请求优先返回进行中或失败恢复，不再并行启动多条模型流。旧 `/api/session/round_complete`、`/api/session/b`、`/api/session/c` 已废弃并返回 410，避免旧接口绕过扣次或重复触发摘要模型。这样优先避免“回答已归档但用户收不到完成态”的卡死，同时收紧重复调模型 / 重复扣费风险；连续数据库异常或进程在归档后扣减前崩溃且短重试也失败时，仍可能漏记一次成本，列入未关闭风险观察
- B 层通用短期记忆 / C 层长期通用记忆当前由后端 `SummaryService` 异步触发 Qwen3.5-Flash，并显式设置 `temperature=0.8`、显式关闭思考模式；单次摘要提取请求有 60 秒超时保护，避免非流式模型请求极端挂住后长期占用同用户同层 `running` guard。B 层仍按 Free / Plus 每 6 轮、Pro 每 9 轮触发并使用当前 A 层 6/9 轮窗口更新；C 层仍每 20 轮触发并读取 `session_round_archive` 最近 20 轮完整问答 + 旧 C 层记忆重写，不再用 A 层 6/9 轮窗口替代 20 轮归档；如果归档不足 20 轮，则保持 `pending_retry_c`，后续轮次完成后继续补提取。同一用户同一层有本进程内运行中保护，避免并发重复抽取；写回时会校验 `session_ab.round_total` 必须仍等于触发时快照轮次，旧快照结果不会覆盖更新轮次。若处理过程中又有新轮次完成，旧结果写入会被跳过，pending 状态保留到后续触发继续处理；模型请求失败、超时、写回失败或归档读取失败会保持对应 `pending_retry_b / pending_retry_c`，后续轮次完成后继续补提取
- 当前工作线视觉 gap 为 `96.dp`，也就是小球、streaming 正文底边、开机历史态和完成态尾部都应落在 composer 折叠外壳上方约 96dp 的位置；工作线以下的空白必须露出来，用于免责声明 / 极端说明 / 底部呼吸区，不能把尾部文字压到输入框后面

## 聊天 UI 主链

- `ChatRecyclerViewHost.kt` 当前是正向 `LazyColumn`，没有 `reverseLayout`，没有 `items.asReversed()`
- `ChatRecyclerViewHost.kt` 默认使用 `verticalArrangement = Arrangement.Bottom`，确保短内容不满一屏时也贴在底部工作线附近；唯一例外是清数据 / 删除历史后的首次真实业务内容尚未碰到工作线、以及触线后短暂安全接底部锚点期间，`ChatScreen.kt` 用 `InitialWorklinePhase.TopUnreached / TopAnchoring` 临时切到 `Arrangement.Top`，让真实用户消息、图片 pending / 失败、assistant placeholder 和 streaming 小球从顶部自然向下排。`TopUnreached` 会 gate 普通回底、AutoFollow 预锚和 sendStart bottom anchor；触线判断使用当前首屏文档流最大可测底边（消息 content bounds、streaming bottom、可见消息 item bottom），不是只看最后一条逻辑消息，避免首条超长用户消息把 assistant placeholder 挤到未组合区域后漏触发上抬。文档流底边到达 96dp 工作线后，如果用户正在拖动 / 滚动 / 浏览，直接交回 `WorklineOwned` 且不抢手势；如果用户正在触碰但未浏览，则继续停在 Top 流等待；如果用户未触碰 / 拖动 / 浏览，则进入极短 `TopAnchoring`，继续保持 `Arrangement.Top`。`TopAnchoring` 只有在列表已经出现正向可滚范围、文档流底边已超过工作线约 56dp、且用户没有触碰 / 拖动 / 浏览时，才在同一个执行点设置 `WorklineOwned` 并立刻复用现有强制底部锚点接一次，降低 Top 到 Bottom 直接切换造成的“先掉一下再上抬”。后续发送如果首屏文档流已经触线，发送入口不得降回 `TopUnreached`，必须继续按正常工作线锚点处理。首屏未触线阶段如果用户主动拖动列表，会交回 `WorklineOwned` 并进入 `UserBrowsing`，不抢用户手势；发送起步只在真实拖动 / 滚动时保留 `UserBrowsing`，不再让 stale 浏览态拦住“你好”等后续发送的工作线 AutoFollow。本地聊天窗口快照会保存 `initialWorklineOwned`，首屏未触线时被系统杀掉再进入仍恢复为未触线阶段，真正交给工作线后才持久化为 owned。`HandoffPending` 已从运行时状态机删除；`SparseBottomSpacer`、`cleanStateSparseLayoutActive`、动态稀疏 padding / spacer、反向列表、overlay 和 raw delta 仍是废弃旧方案，后续不能作为并存旧方案恢复；今日农情卡片在 `TopUnreached / TopAnchoring` 临时隐藏，避免 UI-only 卡片影响工作线判断
- `ChatScreen.kt` 当前使用 `ChatTimelineItem` 作为列表展示层，允许插入一个 UI-only 今日农情卡片；真实业务消息仍只来自 `messages`，不再通过 `chatListItems` 派生 streaming block item
- streaming 小分割 / block item 化已撤掉：`StreamingBlockChatListItem / StreamingTextBlock / streamingBrowseBlockSnapshot / activeStreamingBlockIndex / streaming_tail` 等符号在主链无残留
- mixed active-zone / overlay 运行时已退出主链：
  - `StreamingLocation`
  - `BottomActiveZoneSlice / resolveBottomActiveZoneSlice(...)`
  - `renderBottomActiveZone()`
  - active-zone 拖动接管 / Overlay 恢复门
  - `requestSendStartBottomSnap()`
- 当前禁止恢复 streaming 高度追滚补偿：`followStreamingByDelta(...)`、`scrollBy(...)`、`dispatchRawDelta(...)`、`streamBottomFollowActive` 不在主链运行
- 回到底部 / AutoFollow 使用最新消息 `lastIndex + FORWARD_LIST_BOTTOM_SCROLL_OFFSET`；该 offset 依赖 Compose 正向列表 positive `scrollOffset` 会把 item 继续向上推并在列表末端 clamp 的语义
- 正向列表传给 `LazyColumn` 的 bottom padding 会扣掉 `CHAT_MESSAGE_ITEM_VERTICAL_PADDING`，只补偿消息 item 外层 padding，不改变工作线本身
- 每次 reveal 提交前，AutoFollow 会先请求一次最新消息底部锚点；提交新的 `streamingMessageContent` 后，`ChatScreen.kt` 顶层通过 `SideEffect` 在同帧 apply changes 后、layout 前再次请求最新消息底部锚点，专门压正向列表下一行先从工作线下方冒头的一帧
- 大白话口径：以前是“新字 / 新行先长出来，列表下一拍才把它拉回工作线”，所以用户能看见下面冒一下；现在是“新字 / 新行要长出来的同一拍，先把列表底部钉在工作线”，画到屏幕时已经归位，所以冒头闪被压住
- 高频 reveal 底部锚点请求带 generation 守护，一帧后只允许最新请求关闭 `programmaticScroll`，避免旧取消任务把新程序滚动提前关掉后被误判成用户浏览
- 启动显示门不再把本地已有消息 / 首次欢迎空态硬等到 hydrate barrier 后才显示；有本地消息、已有 streaming item 或尚未开始过对话时，列表/欢迎壳可以先显示，减少开机白屏时间。历史消息贴底仍走正向列表最新消息 `lastIndex + FORWARD_LIST_BOTTOM_SCROLL_OFFSET` 主链
- 静态 / 开机 / 完成态到底必须同时满足文本底边命中 96dp 工作线以及 `chatListState.canScrollForward == false`，避免“文本看似贴线，但工作线以下空白还没完整露出、还能继续往上扒”的状态被误判为到底
- 首屏历史贴底恢复成多帧确认：等 `startupLayoutReady` 和底部固定 composer 宿主稳定实测高度都到位后最多连续重试 6 帧，只有文本底边命中 96dp 工作线且 `canScrollForward == false` 时才把 `initialBottomSnapDone` 记完成；贴底刚完成后如果 stable bottom reserve 又更新，且用户还没开始新对话 / 没触碰滚动，会再做一次非动画回底修正，避免一次 `scrollToBottom(false)` 尚未真正露出底部空白就关门
- 开机历史态 / 完成态在输入为空、无 focus、IME 收起、composer 非 settling、非发送锁的折叠稳定窗口中，列表 bottom padding 优先吃底部固定 composer 宿主的稳定实测高度再加 96dp 工作线 gap，不再只靠启动估值 / 旧观察值；这是为了保证工作线以下空白完整露出来，同时避免 IME 动画帧进入列表测量链
- 用户进入 `UserBrowsing` 后，如果用户明确滑回正向列表物理底部（`canScrollForward == false`、手指已抬起、列表已停止），会先请求一次正向底部锚点再恢复 `AutoFollow`；连续 2 帧工作线稳定命中只保留为兜底，避免 streaming 持续吐字打断容差导致手动回底后长时间不跟随
- 回到底部按钮仍保留 56dp 安全区：用户滑动过程中不显示，停止滑动后如果正向列表仍可向前滚动且最新消息底边离 96dp 工作线超过安全区，才短暂出现；点击后滚到最新消息 `lastIndex` 并恢复对应滚动模式
- 2026-04-28 用户真机反馈：当前正向列表滚动链整体“确实很稳”，小米 / MiMo 会诊后落地的 SideEffect 同帧锚定、物理底部恢复 AutoFollow、96dp 工作线贴底等主链规则继续作为当前稳定基线保留；后续不要再恢复反向列表、小分割、overlay 或 raw delta

## 渲染与收口

- waiting 小球、streaming 正文、settled 完成态共用同一条 assistant 消息 item，不再切第二滚动主人
- `ChatStreamingRenderer.kt` 当前 active streaming 和 settled Markdown 都走 soft-wrap block renderer，并复用 inline Markdown cache 保留加粗 / 链接 / code；旧 committed 物理行预切 / TextMeasurer 渲染链已移除，避免 streaming -> settled 收口时换渲染模型导致行高 / 行宽微动
- active Markdown 仍实时吐字，但 `# ` / `- ` / `1. ` / `> ` 这类结构前缀必须等后面已有非空正文才结构化，避免只有符号的半成品先变标题 / 列表再重排
- streaming reveal 当前对中文通常 1 到 2 个字一拍，英文 / 数字仍按词块吐出，减少“几个中文字一坨蹦出来”的体感；仍不恢复新字尾部灰色高亮动画，也不把吐字频率推到每个汉字都单独重组
- 标准 Markdown 表格当前不做真表格控件，renderer 会把表格行降级成普通项目行文本，保证模型偶发输出表格时至少可读、不撑乱聊天布局；代码块内的 `|` 不做表格降级。emoji / 表情若偶发输出，继续按普通文本由 Compose `Text` 承接
- streaming 期间不提前显示免责声明文字；如果内容已满足免责声明触发条件，只预留同等几何高度，settled 后才显示真实文案，避免尾部收口当拍突然增高
- 完成态收口继续保留两阶段 finalize：
  - `beginPendingStreamingFinalize(...)`
  - fresh bounds 到位后 `finalizeStreamingStop(...)`
  - 不回退成同拍直接切 settled
  - 不在 finalize 当拍恢复旧 bottom align 精修；若仍处于 AutoFollow，只在 fresh bounds 到位后请求一次正向底部锚点，避免长回复完成时窗口被重新锚到上方

## 当前调试焦点

- 这轮正向列表的目标，是牺牲反向列表那套“最新 item 天然视觉底部”的物理模型，换回用户上滑浏览时更稳定的正向滚动体感
- 输入框发送收口当前已取消旧高度锁：发送时不再用发送前的输入内容高度 / chrome 高度撑住 `composerSettlingMinHeightPx` / `composerSettlingChromeHeightPx`，优先让 composer 随输入清空直接回到空态高度；所有主动收键盘路径统一只调用 `focusManager.clearFocus(force = true)`，不再同帧额外调用 `keyboardController.hide()`，避免部分输入法在双触发下出现多一拍残影。已经没有显示入口的 composer collapse overlay prewarm snapshot 协程也已删除，后续若仍有明显迟钝，再单独评估 `WindowInsetsAnimationCompat`，不先动消息列表主链
- DEBUG 包新增 `ChatStartup` 诊断日志，用来区分清数据 / 重装后看到旧内容时到底来自本地 `chat_ui_cache`、本地 streaming draft，还是后端 `SessionApi.getSnapshot()` hydrate。日志不参与 release 行为，不改变 UI 逻辑；真机排查时可用 `adb logcat -s ChatStartup`
- 异常与生命周期兜底当前继续围绕“不中断主滚动链”小范围加固：日额度耗尽后，当前本地会话当天会把发送键置灰但保留点击提示；无网发送纯文字时会生成失败用户气泡并同步清空输入框和草稿，后续即使用户重新输入同文也会优先复用已有失败用户消息，不再刷出多条相同失败消息；文字 / 图片发送起点会同步保存本地聊天窗口，降低刚点发送就杀 App 时尾部用户消息或 assistant placeholder 来不及落盘的概率；后端 streaming 进程被杀后若远端 snapshot 追不回答案，会在用户消息下方补一个 `回复未完成 · 点击重试` 的 assistant 失败入口；远端历史模式下，`/api/chat/stream` 打到后端后会由后端继续读完上游并在 DONE 后写入 `session_round_archive`，Android 切后台 / 锁屏再回来时会围绕当前 `clientMsgId` 持续拉取 `/api/session/snapshot`，若后端已完成则直接恢复完整 AI 回复，冷启动发现尾部用户消息缺少 AI 回复时也走同一恢复窗口；带图片发送当前已接入 WorkManager 延迟兜底：用户消息上屏并落盘后会按 `chatScopeId + userMessageId` 排一个唯一后台任务，前台活跃时后台只重试不抢跑，前台一旦开始 `/api/chat/stream` 会写入 10 分钟远端启动保护窗；如果 assistant 失败态已经有远端图片 URL，用户点重试时也会重新登记同一条 pending，让 App 立刻被杀后仍有后台补发机会；后端同步使用 `chat_stream_inflight` 进行中锁和 lease token，并通过数据库唯一约束限制同一 `user_id` 同时只有一条活跃主流式请求，避免不同 `client_msg_id` 并发打穿额度预检查并多开主模型。重复请求在完成归档前返回 `409 STREAM_IN_PROGRESS` 给客户端走长窗口 snapshot 恢复，完成后的 replay 真源以 `session_round_ledger` / 归档成功为准，服务端只在轮次归档成功后才向客户端发送 SSE `[DONE]`，额度扣减也在归档成功后执行，避免 quota ledger 已写、客户端已收完成态但回答未归档时返回空 replay；旧请求释放时也只能释放自己的锁。前台 SSE 会识别 replay 事件，不会把 replay + DONE 当成空回复完成。若前台上传失败并已经显示“发送失败”，对应后台任务会取消，不会一边显示失败一边后台偷偷消耗额度。切后台 / 锁屏时仍会收起输入焦点并清掉消息 / 输入选择菜单，避免回来还挂着复制黑卡片；`SessionApi` 的当前 SSE call 会等响应读循环退出后再清空引用，保证 reset / cancel 能尽量取消正在读的远端流
- 当前最需要真机验证的是：
  1. 首屏进入有历史时是否稳定贴底
  2. 发送瞬间小球是否第一时间出现在工作线，历史文本是否不抖
  3. streaming 工作线下一行冒头闪已由用户初测确认被 `SideEffect` 同帧锚定压住，后续只需继续观察长回复 / 不同机型是否复现
  4. 用户上滑 / 下滑是否不抢手，想停哪里能停哪里
  5. 用户点击回到底部、或手动下滑到物理底部后是否恢复 AutoFollow
  6. finalize 收口是否稳定，尤其含免责声明答案是否不再尾部增高微跳
  7. 输入框上方和静态文本底部是否不再出现额外白块
  8. 冷启动首次点输入框、键盘弹起 / 回缩是否比旧 `SubcomposeLayout` 版本更利索；如果仍有残影，下一步只评估 `WindowInsetsAnimationCompat` 或输入法平台差异，不先动消息列表主链

## 当前交接入口

- 主规则：`AGENTS.md`
- Android 局部补充：`app/AGENTS.md`
- Go 后端局部补充：`server-go/AGENTS.md`
- 当前风险：`docs/project-state/open-risks.md`
- 待定事项：`docs/project-state/pending-decisions.md`
- 近期变更：`docs/project-state/recent-changes.md`
- 关键决策：`docs/adr`
- 运维与排障入口：`docs/runbooks`
- 下一阶段上线推进计划：`docs/runbooks/go-live-plan.md`

## 当前阶段判断

- mixed active-zone / overlay 架构经过多轮补丁后，已经证明不适合作为最终方案；继续补只会在“发送抖、拖动乱窜、消息重叠”和“闪烁 / 收口抖”之间来回换问题
- 反向列表单主人虽然压住了大部分底部冒头闪，但在最新 assistant item 内小幅上滑时仍会被动态长高锚点带一下，用户体感不可接受
- 当前仓库已按用户拍板切回“**单一运行时主人 + 正向列表**”路线；这不是恢复旧正向 overlay / placeholder / 追滚补偿，而是：
  - 正向 `LazyColumn`
  - 保留 send-start 保护窗口
  - 保留两阶段 finalize
  - 保留 unified soft-wrap renderer
  - 删除 mixed active-zone、小分割 itemization、streaming raw delta / scrollBy 追滚
