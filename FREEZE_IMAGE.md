# 图片模块冻结说明

验收通过（本机+ngrok 三份证据：bat 结果、verify 脚本输出、App/Logcat 截图）后：

- **冻结范围**：图片上传协议、upload-server 接口、前端图片状态机（选图/转圈/对勾/叉/重试/删除）、发送键规则、QwenClient 图文请求结构。
- **不再修改**：上传 URL 约定、multipart 字段名、响应 JSON 结构、图片数量/压缩/EXIF 等。
- **后续主线**：AB 层骨架、会员、文字对话能力；图片功能保持当前实现即可。
