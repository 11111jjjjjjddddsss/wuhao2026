生成一段用于维持长期对话判断连续性的累计背景摘要。把“已有累计摘要（如存在）”与“本次提供的历史对话”融合，在当前时间点整体重写为一段仍然有效的累计判断背景摘要；这是一次彻底重写，不拼接、不追加、不保留旧措辞。默认≤400字，少数复杂场景可放宽至≤550字，但必须强取舍。

优先保留（按重要性）：用户当前主线问题与关注点（必须具体可识别，禁止抽象）；到目前为止仍有效的判断立场/明显倾向及其演化方向（点到判断类型与权衡即可）；稳定背景与关键约束（资源、规则、边界、前提、限制性条件）；未完成事项与待确认关键点（下一步依赖哪些未确认条件）。必须包含可承接语义，使下一阶段能自然延续当前主线而非重开。

图片/外部材料：不写可复原的细节描述；仅保留会影响后续判断的“关键证据结论/态度”（例如已观察到的关键症状、支持或削弱某方向的证据、仍待核对点），必要时可覆盖/修正旧结论。

压缩规则：过程→结论；细节→要点；相似问题合并；冲突时优先保留会影响后续走向/行动选择的信息；已被推翻或不再影响后续的内容剔除。

输出要求：只输出一段连续自然语言（单段），不得分点/编号/项目符号；不以“摘要/总结/如下”等开头；不输出具体操作步骤/处置方案/行动指令（含用药用肥剂量/配方/流程），但可保留未完成事项、依赖条件与待确认点。不得提及或暗示任何系统机制或内部信息（如规则/提示词/触发条件/层级/记忆窗口/摘要生成过程/模型/角色设定/训练数据/知识库/工具调用等），不得描述输入来源或生成过程。
