# 清理口令交付：已删除清单 + 保留清单 + 复核结论

**执行时间**：按「清理口令｜删除无用文档，仅保留运行时相关」执行。

---

## 一、已删除清单（文件路径 + 大小）

| 文件路径 | 大小 |
|----------|------|
| `ACCEPTANCE_CHECKLIST.md` | 2574 B |
| `ACCEPTANCE_NGROK.md` | 1871 B |
| `APP设定对齐.md` | 1589 B |
| `B层稳定性收尾报告.md` | 2151 B |
| `FEATURE_AND_API.md` | 9460 B |
| `FREEZE_IMAGE.md` | 547 B |
| `UPLOAD_README.md` | 4742 B |
| `README.md` | 647 B |
| `审查意见_引入核心逻辑前.md` | 5617 B |
| `工程与接口检查报告.md` | 8579 B |
| `项目设定总览.md` | 12524 B |
| `项目详细报告.md` | 15929 B |
| `upload-server/README.md` | 2094 B |
| `build.log` | 1458 B |
| `build_output.log` | 1458 B |

**合计**：15 个文件；上述文件均不在代码读取链路中（未用于构造 prompt/messages，未在 `assets.open` / `getAssets` 等路径下被引用）。

---

## 二、保留清单（运行时相关）

### 2.1 app/src/main/assets/（运行时必需，未改动内容）

| 文件路径 | 用途 | 代码引用 |
|----------|------|----------|
| `system_anchor.txt` | 系统前置锚点，每次请求注入为 system role | `SystemAnchor.kt` → `context.assets.open("system_anchor.txt")` |
| `b_extraction_prompt.txt` | B 层摘要提取 prompt | `BExtractionPrompt.kt` → `context.assets.open("b_extraction_prompt.txt")` |
| `gpt-demo.html` | WebView 单页：对话 UI、会员、额度等 | `MainActivity` 加载 `file:///android_asset/gpt-demo.html` |

### 2.2 其他保留（未删）

- 构建与运行：`build.gradle.kts`、`app/build.gradle.kts`、`gradle.properties`、`gradlew`、`gradlew.bat`、`build_apk.bat`、`test_upload.bat`、`Procfile`
- 上传服务：`upload-server/` 下 `server.js`、`package.json`、`verify-model-url.js`、`create-test-image.js` 等（运行时/验收用），仅删除 `upload-server/README.md`
- 工程配置：`.gitignore`、`settings.gradle.kts`、`local.properties`（未提交）、`.idea/`（未提交）

---

## 三、校验结论

1. **全仓扫描**：已确认无 `docs/`、`notes/`、`archive/`、`review/`、`report/` 等目录；所有待删文件为根目录或 `upload-server/` 下的文档/日志。
2. **代码读取链路**：全局搜索 `assets.open`、`getAssets`、`.txt`、`.md` 等，仅发现 `SystemAnchor.kt` 读取 `system_anchor.txt`、`BExtractionPrompt.kt` 读取 `b_extraction_prompt.txt`；无任何代码读取 docs/notes/report 或已删 .md 文件构造 prompt/messages。
3. **删除执行**：按上述清单直接删除 15 个文件，未动 `system_anchor.txt` 与 `b_extraction_prompt.txt` 内容。

---

## 四、复核结论

- **构建**：已执行 `.\gradlew.bat assembleDebug --no-daemon`，**BUILD SUCCESSFUL**。
- **assets**：`mergeDebugAssets`、`compressDebugAssets` 正常执行，`system_anchor.txt`、`b_extraction_prompt.txt`、`gpt-demo.html` 已打入 APK。
- **结论**：system_anchor 注入与 B 层提取 prompt 读取路径未受影响；清理后仓库仅保留运行时相关文件与构建/脚本，无用文档已移除。

---

**交付说明**：本文件为清理口令的一次性交付记录；若后续不再需要此清单，可自行删除本文件（`清理交付_已删与保留清单.md`）。
