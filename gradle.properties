org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
# Android 客户端不再注入模型服务 Key；主模型 / 摘要模型调用只能经由后端完成
# 图片上传后端地址（可选）。配置后 APP 将 POST multipart 到 {UPLOAD_BASE_URL}/upload，期望 JSON 返回 { "url": "https://..." }；禁止在 APP 内写 OSS AK/SK
# UPLOAD_BASE_URL=https://your-backend.example.com
