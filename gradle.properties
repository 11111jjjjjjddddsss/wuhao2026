org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
# Android 客户端不再注入模型服务 Key；主模型 / 摘要模型调用只能经由后端完成
# 图片上传 / 账号登录 / 聊天后端地址由 app/build.gradle.kts 固定为生产 HTTPS API；
# 不再通过 Gradle 属性或环境变量切换业务后端，APP 内禁止写 OSS AK/SK 或模型 Key
